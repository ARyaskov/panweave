[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Panweave repository automation (conformance rendering, quality gate, code generation)"

[dependencies]
toml = { workspace = true }
serde = { workspace = true, features = ["std"] }
