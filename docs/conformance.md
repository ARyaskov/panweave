# Conformance status

Generated by `cargo xtask conformance` from `conformance/*.toml`. Do not edit by hand.

Panweave is an independent implementation; this table records the maintainers' own assessment against the referenced specification sections and is not a certification claim.

## Summary

| Document | Total | Implemented | Partial | Not implemented | N/A | HW validation | Clarification |
|---|---|---|---|---|---|---|---|
| BDB3.1 | 27 | 26 | 0 | 0 | 1 | 0 | 0 |
| DTL2 | 3 | 2 | 0 | 0 | 0 | 0 | 1 |
| ZD1.1 | 12 | 10 | 2 | 0 | 0 | 0 | 0 |
| GP1.1.2 | 9 | 9 | 0 | 0 | 0 | 0 | 0 |
| SE1.4a | 18 | 13 | 3 | 0 | 0 | 1 | 1 |
| ZCL8 | 48 | 47 | 0 | 1 | 0 | 0 | 0 |
| R23.2 | 133 | 128 | 2 | 1 | 2 | 0 | 0 |
| **All** | 250 | 235 | 7 | 2 | 3 | 1 | 2 |

## BDB3.1 — PRO Base Device Behavior Specification v3.1

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-BDB-CFG-001 | §5.1–§5.7 | mandatory | implemented | `panweave-bdb` | BDB constants (primary/secondary channel masks, scan durations, TCLK exchange attempts, min commissioning time, touchlink constants) and attributes (bdbNodeIsOnANetwork, bdbCommissioningMode, bdbCommissioningStatus, bdbJoiningNodeEui64, bdbNodeJoinLinkKeyType, bdbTCLinkKeyExchangeMethod, bdbTrustCenterRequireKeyExchange, join policy, node configuration) | `formation_falls_back_to_secondary_list`, `steering_retries_then_secondary_then_opens_network` | Config (bdbcf* attributes, node policies) with the 2.4 GHz defaults of Tables 4–7 and 10; constants of Tables 1–3; bdbgNodeIsOnANetwork kept by the machine. Sub-GHz defaults are not provided. |
| PW-BDB-POL-001 | §6.1, §6.2 | mandatory | implemented | `panweave-bdb` | Logical device types and centralized/distributed security models; coordinator forms centralized, router may form distributed | `formation_falls_back_to_secondary_list`, `bdb_formation_steering_finding_binding_and_rejoin` | Formation refused for end devices; coordinators form centralized and routers distributed networks through StackConfig::distributed. |
| PW-BDB-POL-002 | §6.3 | mandatory | not-applicable | `panweave-bdb` | Commissioning mode bitmask (touchlink, steering, formation, finding & binding) processed in the specified order; commissioning status reporting | — | BDB 3.1 no longer defines the bdbCommissioningMode bitmask / bdbCommissioningStatus attribute of BDB 3.0; procedures are invoked individually and report an Outcome. |
| PW-BDB-POL-003 | §6.4 | mandatory | implemented | `panweave-runtime` | Minimum requirements: mandatory ZDO services, identify cluster, groups where required, APS ack usage, node descriptor fields, stack compliance revision | `bdb_formation_steering_finding_binding_and_rejoin`, `router_keep_alive_detects_a_vanished_trust_center` | Profile 0x0104, Identify client for initiators, Groups server with the group table, simple descriptors per endpoint, stack compliance revision 23; the Trust Center's Keep-Alive server and Poll Control client are provided by panweave::endpoints::trust_center_utility and routers run the keep-alive client of ZCL8 §3.18.4 (Match_Desc discovery, APS-encrypted reads, TrustCenterLost after three failures). |
| PW-BDB-POL-004 | §6.5 | mandatory | implemented | `panweave-zcl::attribute` | Default reporting configuration for reportable attributes | `reporting_configuration_and_reports`, `reset_to_factory_defaults_restores_attributes_and_reporting` | DefaultReporting installed at instance creation (OnOff: change-based plus 300 s; CurrentLevel: 1 s minimum, 300 s, change 1) and restored by Reset to Factory Defaults; reports go to bound destinations without further configuration and a Configure Reporting overrides it. |
| PW-BDB-POL-005 | §6.6 | mandatory | implemented | `panweave-runtime::stack` | MAC data polling: end devices poll parent at least at the specified rate; fast polling after transmissions | `application_selects_the_fast_poll_rate`, `poll_control_check_in_puts_the_sleepy_device_into_fast_poll_mode` | Two rates (StackConfig::poll_interval, default 3 s, and fast_poll_interval): fast polls burst after every transmission and while the network key is awaited, Poll Control fast poll mode selects its own short interval, and Stack::set_fast_polling keeps the fast rate while the application waits on the network; the facade Node polls fast while any BDB procedure (steering, finding and binding, rejoin) runs. The rates themselves are product choices (section 6.6 gives recommendations only). |
| PW-BDB-POL-006 | §6.7 | mandatory | implemented | `panweave-runtime::persist` | Persistent data set retained across power cycles | `end_device_and_coordinator_survive_reboots` |  |
| PW-BDB-POL-007 | §6.10 | mandatory | implemented | `panweave-aps::layer` | APS acknowledgement and APS security usage rules for ZCL and ZDO traffic | `trust_center_join_and_link_key_update`, `bdb_formation_steering_finding_binding_and_rejoin` | Acknowledged and APS-secured requests are answered with matching ack request and security (ADR-0005); ZCL responses inherit the acknowledgement request of the command. |
| PW-BDB-POL-008 | §6.11 | mandatory | implemented | `panweave-types::key::InstallCode, panweave-security::key_hierarchy` | Install codes: 18-octet code with CRC-16 check, hashed with AES-MMO to derive TCLK; formats of 8/10/14/18 octets | `install_code_crc_known_answer`, `install_code_known_answer_from_bdb` |  |
| PW-BDB-POL-009 | §6.12 | optional | implemented | `panweave-bdb::setup_code` | Short device setup codes | `example_of_6_12_2` | The §6.12.1 modified base32 alphabet, decoding of a displayed code to the little-endian, zero-padded 128-bit pass code of §6.12.2 (verified against the U4DXS2 example), the SPEKE view of the significant bytes, the display encoding and the 20–30 bit recommendation. Using the pass code as a Trust Center key-pair entry for SPEKE joins goes through the existing dynamic link key negotiation (PW-R23-SEC-* DLK items). |
| PW-BDB-INIT-001 | §7.1 | mandatory | implemented | `panweave-runtime::persist` | Initialization procedure: restore persistent data, if on a network attempt rejoin per §10.1 (routers resume, end devices rejoin), otherwise wait for commissioning | `end_device_and_coordinator_survive_reboots`, `factory_reset_erases_network_state`, `rejoin_walks_the_channel_lists` | Stack::restore + Stack::resume: routers and the coordinator resume on their network and re-announce; end devices run the section 10.1 Rejoin procedure of panweave-bdb (secured rejoin on the current channel, Trust Center rejoin on a centralized network with a verified key, then secured rejoins over the primary and secondary channel lists, RejoinUnsuccessful at the end), which the facade Node::initialize starts and which a lost Trust Center enters at step 2. |
| PW-BDB-INIT-002 | §7.2 | mandatory | implemented | `panweave-runtime::pump` | On-network TCLK update procedure: after joining a centralized network request a unique TCLK (Request Key or DLK), verify it with Verify Key / Confirm Key, retry up to bdbTCLinkKeyExchangeAttemptsMax, leave on failure if required | `coordinator_and_end_device_full_stack` | After a join with a global key on a centralized network the joiner requests a unique TCLK (Request Key), verifies it (Verify Key) and receives Confirm Key; bdbTCLinkKeyExchangeAttemptsMax retries and the DLK variant are not implemented. |
| PW-BDB-INIT-003 | §7.3 | conditional | implemented | `panweave-runtime::keep_alive` | Ensuring TC connectivity via keep-alive cluster, poll control, or node descriptor polling | `router_keep_alive_detects_a_vanished_trust_center`, `router_falls_back_to_node_descriptor_polling`, `poll_control_check_in_puts_the_sleepy_device_into_fast_poll_mode` | The Keep-Alive client (Match_Desc discovery of the Trust Center Keep-Alive server, APS-secured attribute reads at the advertised base / jitter pacing), the Poll Control server / client path, and the section 7.3.3 fallback for a Trust Center without a Keep-Alive server (Node_Desc_req polls at the default pacing) are implemented; three successive failures raise StackEvent::TrustCenterLost. |
| PW-BDB-INIT-004 | §7.4 | optional | implemented | `panweave-runtime` | Application link keys with non-TC devices via Request Key | `trust_center_brokers_an_application_link_key`, `listed_only_policy_refuses_unlisted_pairs` | Stack::request_application_link_key sends APSME-REQUEST-KEY for an application link key; the Trust Center applies allowApplicationKeyRequests (Any / ListedOnly against applicationKeyRequestList / Never) and the requirement that both devices hold verified Trust Center link keys, then transports one fresh key to both (initiator flag on the requester) under their Trust Center link keys. Both sides install it as a verified unique entry and report StackEvent::ApplicationLinkKey; data frames secured with an application link key carry the extended nonce so a partner that only knows the short address can locate the key, and a single-hop secured frame's auxiliary-header source is recorded in the address map. |
| PW-BDB-FORM-001 | §8.1 | mandatory | implemented | `panweave-bdb` | Network formation: coordinator forms centralized network on primary then secondary channels; router forms distributed network with random extended PAN ID and network key; set permit join off; update commissioning status | `formation_falls_back_to_secondary_list`, `bdb_formation_steering_finding_binding_and_rejoin` | Centralized formation on the configured mask with a supplied or CryptoRng-generated network key; secondary mask fallback and distributed formation by routers are not wired. |
| PW-BDB-STEER-001 | §9.1 | mandatory | implemented | `panweave-bdb` | Network discovery procedure across primary then secondary channel masks with bdbScanDuration | `steering_retries_then_secondary_then_opens_network`, `bdb_formation_steering_finding_binding_and_rejoin` | Discovery only considers networks permitting joins with end-device (or router) capacity; suitability beyond the NWK ranking is left to the application. |
| PW-BDB-STEER-002 | §9.2, §9.3 | mandatory | implemented | `panweave-runtime::pump` | Join using symmetric key exchange: select network with permit join, MAC associate, receive network key with preconfigured link key (global or install code), then TCLK update | `r23_trust_center_leads_to_the_symmetric_exchange`, `pre_r21_trust_center_keeps_the_global_key`, `bdb_formation_steering_finding_binding_and_rejoin` | Join, network key delivery, Device_annce and the On-Network TCLK Update procedure of section 10.2.4: Node_Desc_req to the Trust Center with the Supported Key Negotiation Methods TLV and apsSecurityTimeOutPeriod (LinkKeyUpdateFailed on silence), no update below stack revision 21 (LinkKeyUpdateSkipped, which the BDB machine treats as success), the symmetric Request Key / Verify Key exchange below revision 23 or when the Trust Center selects the Zigbee 3.0 mechanism, and the selected key negotiation with the named pre-shared secret otherwise (symmetric fallback when this device holds no such secret). The Trust Center answers Node_Desc_req with the Selected Key Negotiation Method TLV (SPEKE with the device authentication token or install-code key when it holds one, protocol 0 otherwise). The CBKE branch belongs to PW-BDB-STEER-004. |
| PW-BDB-STEER-003 | §9.4, §9.5 | mandatory | implemented | `panweave-runtime::dlk` | Join using key negotiation (DLK) with Network Commissioning Request and Security_Start_Key_Negotiation | `anonymous_key_negotiation_join_through_router`, `install_code_authenticated_negotiation`, `wrong_passphrase_fails_and_restores_the_entry` | Joining with a Network Commissioning Request and the Security_Start_Key_Negotiation / Security_Key_Negotiation exchanges, anonymous (well-known passphrase) or install-code authenticated, through a router parent; failures restore the previous key-pair entry. |
| PW-BDB-STEER-004 | §9.6 | optional | implemented | `panweave-smart-energy::key_establishment` | Certificate-based TCLK exchange (CBKE) when supported | `cbke_over_the_key_establishment_cluster_installs_a_working_link_key`, `the_driver_runs_cbke_after_joining` | The Key Establishment cluster and CBKE machines are in panweave-smart-energy; the facade CbkeDriver runs the exchange over the stack on both sides, starting it by itself after a join when configured (start_after_join) and installing the derived Trust Center link key as the verified unique key; the simulator tests prove the key with APS-secured traffic. The curve arithmetic is host-supplied (ADR-0012). |
| PW-BDB-STEER-005 | §9.7 | mandatory | implemented | `panweave-bdb` | On-network steering: broadcast Mgmt_Permit_Joining_req with bdbcMinCommissioningTime, local permit join; local disabling | `bdb_formation_steering_finding_binding_and_rejoin` |  |
| PW-BDB-STEER-006 | §9.8 | mandatory | implemented | `panweave-bdb` | Off-network steering: discovery, join attempt per network, on success TCLK update, broadcast permit joining, device announce | `steering_retries_then_secondary_then_opens_network`, `bdb_formation_steering_finding_binding_and_rejoin` | Same-network retries (bdbcRecSameNetworkRetryAttempts, capped at bdbcMaxSameNetworkRetryAttempts), primary then secondary lists, commissioning window broadcast + local permit join and bdbcMinFastPollDurationAfterJoin fast polling. The optional 0–5 s random start delay is left to the application. |
| PW-BDB-STEER-007 | §9.9 | mandatory | implemented | `panweave-runtime::dlk` | Device interview procedure (R23): TC retrieves descriptors/configuration before authorising; joiner responds; timeouts | `interview_holds_the_network_key_until_admitted`, `interview_rejection_removes_the_joiner` | The Trust Center withholds the network key until the negotiated key is verified and, with TrustCenterPolicy::interview_joiners, until the application admits the device (Stack::admit_joiner) after its own implementation-specific exchanges; Stack::reject_joiner turns it away; the pending join expires with apsSecurityTimeOutPeriod otherwise. |
| PW-BDB-REJOIN-001 | §10.1 | mandatory | implemented | `panweave-bdb` | Rejoin procedure: secured rejoin on current channel, then all channels, then unsecured TC rejoin if allowed, with back-off and attempt limits; end devices choose parent by link quality | `rejoin_walks_the_channel_lists`, `bdb_formation_steering_finding_binding_and_rejoin` | Secured rejoin on the current channel, TC rejoin on the current channel (centralized, verified key), then secured rejoins over the primary and secondary lists; the lost-TC entry point skips step 1. |
| PW-BDB-FB-001 | §11 | mandatory | implemented | `panweave-bdb` | Finding & binding: target identifies for bdbcMinCommissioningTime; initiator sends Identify Query broadcast, discovers simple descriptors of responders, creates bindings for matching clusters, optionally groups | `finding_and_binding_initiator_binds_matching_clusters`, `finding_and_binding_group_and_failures`, `bdb_formation_steering_finding_binding_and_rejoin` | Utility clusters (ZCL8 chapter 3 classification) are never bound; unicast bindings resolve the IEEE address with IEEE_addr_req when unknown; group bindings send Add Group to the respondent. |
| PW-BDB-TL-001 | §12 | optional | implemented | `panweave-bdb::touchlink` | Touchlink initiator and target procedures (inter-PAN scan, identify, network start/join, key transport with touchlink key) | `ranges_assign_and_split`, `factory_new_end_device_starts_a_network_with_a_router_target`, `initiator_on_a_network_joins_a_router_and_respects_stealing_policy`, `same_network_reconciles_update_id_and_reset_sends_the_request`, `target_ignores_weak_scans_wrong_transactions_and_expires`, `factory_new_remote_starts_a_network_through_a_lamp_and_adds_another` | Initiator (§12.1): random non-zero transaction identifier, five scan requests on the first primary channel then one per remaining channel, the optional secondary set, candidate collection with RSSI correction, device information and identify requests, the same-network nwkUpdateId reconciliation (§13.3.4.9/10 wrap rule) with Network Update Request, the centralized-network and address-assignment checks, Network Start Request from a factory new end device to a router target (network address 0x0001 for itself, 0x0002 onwards for targets, group and free-range splitting per §13.3.4.8) followed by adoption of the responded parameters and a secured rejoin after aplcMinStartupDelayTime, Network Join Router / End Device Requests from an initiator on a network, aplcRxWindowDuration timeouts, the touchlink reset request. Target (§12.2): RSSI threshold and link-initiator checks, the transaction lifetime, a stable response identifier per transaction, scan responses with the device information table, network start (parameters chosen when the initiator leaves them open) and join under the stealing policy (refused with status 1 on centralized networks unless allowed), network update, reset to factory new. Runtime: inter-PAN frames through the MAC, distributed network formation / adoption with the assigned address, end-device rejoin. Not done: a factory new router initiator (it forms through the BDB formation procedure, step 19), NLME-ADD-NEIGHBOR of the initiator (the rejoin adds it as a child), binding links of step 25, and the master key (index 4) is only used when the application supplies it. |
| PW-BDB-RESET-001 | §13.1–§13.3 | mandatory | implemented | `panweave-runtime::persist` | Reset to factory new via Basic cluster Reset to Factory Defaults, touchlink reset, or local action: leave network, clear persistent data | `factory_reset_erases_network_state`, `reset_to_factory_defaults_restores_attributes_and_reporting`, `factory_new_remote_starts_a_network_through_a_lamp_and_adds_another` | Basic cluster Reset To Factory Defaults resets the attributes of every cluster instance (reporting configuration included) and nothing else (section 13.1); the touchlink Reset To Factory New Request makes the target leave and, once the leave completed, clear its persistent data (section 13.2); Stack::erase_persisted is the local action (section 13.3). Every path keeps the outgoing NWK frame counter record, which Stack::restore reloads even on a factory-new device. |
| PW-BDB-RESET-002 | §13.4, §13.5 | mandatory | implemented | `panweave-runtime::stack` | Removing a node: TC uses Mgmt_Leave / Remove Device on centralized networks; any router on distributed | `trust_center_removes_a_device_through_its_parent`, `any_router_removes_a_node_with_mgmt_leave` | Stack::remove_node: on a distributed network any router sends the node a Mgmt_Leave_req (section 13.5); on a centralized network the Trust Center sends a NWK Leave to its own child, an APS Remove Device to a router itself or to an end device through its parent (section 13.4), and a Mgmt_Leave_req to a remote end device whose parent is unknown. Stack::remove_device remains the raw section 4.6.3.6.1 primitive. Receivers leave on Mgmt_Leave_req (ZDO) and Remove Device (APS). |

## DTL2 — Device Type Library Specification, Revision 2

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-DTL-GEN-001 | §1.9–§1.13 | mandatory | requires-clarification | `panweave-device-library` | Device definition conventions: attribute lists, permitted transmission modes, device class, groups/scenes clarifications, cluster usage restrictions, transmission handling | `classification_and_finding_binding_roles`, `validation_reports_deficiencies` | Device classes (Simple / Dynamic / Node) and the cluster classification used for finding & binding are modelled; the §1.11.1 exemption (a sleepy device may omit the Groups and Scenes servers) is applied by panweave::endpoints::validate_for. The per-device attribute lists' Scene Table / Reportable columns and the permitted transmission mode codes (§1.9) are not recorded in metadata/devices.toml: the check-mark glyphs are lost in the specification text available to this project (requires-clarification for those columns); attribute reportability follows the ZCL cluster definitions instead. The §1.11.2 all-or-none extension field set rule binds scene commissioners (clients) and is not enforced by the server. |
| PW-DTL-DEV-001 | §4–§40 | mandatory | implemented | `panweave-device-library` | Device types with device id and mandatory/optional server and client clusters (switches, outputs, scene selector, configuration tool, remote control, combined interface, range extender, mains outlet, door lock and controller, consumption awareness, home gateway, smart plug, white goods, meter interface, lights, light switches, sensors, thermostats, window covering, IAS devices and others) | `light_and_switch_endpoints`, `appliance_and_energy_device_types`, `every_buildable_device_type_validates` | All 47 device types of Table 3 with their mandatory server / client clusters (from the PICS tables) are generated from metadata/devices.toml; optional clusters are not recorded. The facade instantiates every cluster a device type lists (endpoint builders for the lights, switches, sensors, thermostat, window covering, door lock, ballast, on/off sensor, IAS devices, White Goods and the Trust Center utility endpoint; Home Gateway, Smart Plug, Meter Interface and Consumption Awareness need the smart-energy feature for the Metering cluster of panweave-smart-energy and report it through unsupported_clusters without it). Every buildable device type passes endpoints::validate. |
| PW-DTL-VAL-001 | §1.4, per-device cluster requirements | mandatory | implemented | `panweave::endpoints` | Validation of endpoint composition against device type requirements (required clusters, attributes, commands) | `missing_clusters_are_reported`, `every_buildable_device_type_validates`, `validation_reports_deficiencies` | DeviceType::missing / conforms validate the mandatory cluster lists of a simple descriptor; panweave::endpoints::validate additionally checks that every declared cluster has an instance and that server instances carry the unconditionally mandatory attributes and both sides receive the mandatory cluster-specific commands of panweave_zcl::requirements (ZCL rev 8 M/O columns, conditional M* items excluded). Every buildable device type of the library passes. |

## ZD1.1 — Zigbee Direct Specification, Revision 1.1

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-ZD-SEC-001 | §6.2, §6.3 | mandatory | partially-implemented | `panweave-direct::auth,panweave::direct` | ZDD security states (unprovisioned/provisioned), session types (provisioning, authorization, BLE secure), state transitions | `annex_b3_authorization_keys`, `the_trust_center_configures_the_zdd_interface` | Session types follow from the pre-shared secret (Psk::is_provisioning), authorization levels are modelled (auth::Level) and the Basic / Admin keys are derived per Table 2 (Annex B.3 vectors); the Responder refuses secrets the application does not offer. The facade keeps the interface state and the Anonymous Join Countdown Timer (Node::anonymous_join_allowed: only while the network is open and the countdown of §11.3.5.4.4 runs, restarted on power-up and reconfiguration) and the Zigbee Direct aware check of §6.2.3 (Node::check_direct_aware, DirectState::trust_center_aware; PW-ZD-ZCL-001). The provisioned / unprovisioned ZDD state machine, the one-authorization-session rule and the BLE OFF state are left to the ZDD integration. |
| PW-ZD-SEC-002 | §6.5.1.2 | mandatory | implemented | `panweave-direct::session` | Session establishment ECDHE-PSK/P-256/SHA-256/HMAC-SHA-256-128: four messages carrying method and public point TLVs, key derivation and MAC tag confirmation | `annex_b1_ecdhe_p256_vectors`, `responder_refuses_wrong_method_and_psk` | Initiator (ZVD) and Responder (ZDD) machines for the four Session Establishment messages; P-256 ECDH with SHA-256 shared-secret hashing over the ordered session identifier, HMAC-SHA-256-128 session key and 32-octet MacTags (KC_2_U / KC_2_V); every Annex B.1 vector reproduced. Feature p256. |
| PW-ZD-SEC-003 | §6.5.1.3 | mandatory | implemented | `panweave-direct::session` | Session establishment SPEKE/Curve25519/AES-MMO-128/HMAC-AES-MMO-128 | `annex_b2_speke_curve25519_vectors` | SPEKE with the PSK-derived generator, AES-MMO-128 hashing and HMAC-AES-MMO-128 (shared with the R23 DLK primitives of panweave-security), 16-octet MacTags; Annex B.2 vectors reproduced. Feature curve25519. SPEKE/Curve25519/SHA-256 stays reserved (Method 2 is decoded but refused). |
| PW-ZD-SEC-004 | §6.4 | mandatory | implemented | `panweave-direct::secure` | Secured characteristic payloads with CCM, nonce from unique address and counters, counter monotonicity, exception handling | `annex_b4_join_network_write` | SecureChannel keeps the session key and the incoming / outgoing counters: AES-CCM-128 with the 34-octet unique address of the characteristic as associated data, the nonce of §6.4.3, counter \|\| ciphertext \|\| MIC framing, freshness (strictly increasing counter) and authenticity checks that leave the incoming counter untouched on failure, and CounterExhausted at 0xffffffff; Annex B.4 reproduced. The ATT error responses themselves belong to the BLE host integration. |
| PW-ZD-SEC-005 | §6.5.2 | mandatory | implemented | `panweave-direct::tlv` | Security service local TLVs (method, P-256 point, Curve25519 point, network key sequence number, MacTag) | `annex_b1_ecdhe_p256_vectors`, `annex_b2_speke_curve25519_vectors` | Key Negotiation Method (method + PSK enumerations), P-256 and Curve25519 Public Point (EUI-64 + point), Network Key Sequence Number and MacTag TLVs in the Annex I TLV format. |
| PW-ZD-ZDD-001 | §7.6 | mandatory | implemented | `panweave-direct::advertisement` | ZDD BLE advertisement extension (service UUID, network status, PAN identifier data) | `round_trip_and_not_joined_defaults` | Service Data AD structure with the Commissioning Service UUID, version 1, ZDTS / PermitJoin flags, PAN ID and NWK address (0xffff when not joined); encode and find-in-advertisement. |
| PW-ZD-ZDD-002 | §7.7.2 | mandatory | implemented | `panweave-direct::commissioning,panweave::direct` | Commissioning service characteristics: form network, permit joining, join network (MAC association / rejoin / out-of-band), leave, commissioning status, manage joiners, identify, finding and binding; TLV items | `join_network_tlv_usage_rules`, `status_round_trip`, `zdd_side_authorization_and_flow`, `zdd_form_open_join_adopt_and_leave`, `an_out_of_band_join_updates_a_provisional_link_key_when_the_trust_center_answers`, `the_zvd_chooses_the_security_model_of_a_formed_network` | Table 27 / Table 44 TLV codecs, the Form/Join/Permit/Leave/Commissioning Status/Manage Joiners/Identify/Finding & Binding payloads with the joining-method TLV usage rules of Tables 39-41, the authorization matrix of Table 26 and Status Code notifications are in panweave-direct::commissioning behind the Zdd trait; the facade Node implements Zdd (panweave::direct, feature direct): formation with the ZVD's parameters and the security model the Trust Center Address TLV selects (Stack::set_security_model: a coordinator-capable ZDD forms either kind, a router only distributed; §7.7.2.5), joining by association / rejoin / out-of-band adoption, permit joining, leave, Manage Joiners provisional link keys, Identify and Finding & Binding, with completion reported through Event::Direct. After an out-of-band join with a provisional Trust Center link key the ZDD runs the On-Network TCLK Update procedure (Stack::update_trust_center_link_key), stays on the network when it fails and retries every TCLK_UPDATE_RETRY (60 s, an implementation choice) until the key is verified (§7.7.2.7.4). The Admin key of §6.3.2.1 / §7.7.2.7.1: Node::admin_key_for(zvd) is the key a Form / Join Network write provisioned (persisted under Kind::DirectAdminKey until a factory reset) or, on a centralized network, the key derived from the active TCLK; none on a distributed network without a provisioned key. The BLE transport (characteristic reads / writes / notifications) is the host's. |
| PW-ZD-ZDD-003 | §7.7.3, §7.7.4 | mandatory | implemented | `panweave-direct::tunnel,panweave-nwk::layer,panweave::direct` | Tunnel service NPDU characteristic, NPDU message TLV, trusted-link pre/post processing in the NWK layer, joining centralized/distributed networks on behalf of the ZVD, ZVD as trust center, ZVD rejoin/forming | `npdu_message_round_trip_and_session_rule`, `a_virtual_device_joins_and_exchanges_data_over_a_trusted_link`, `zvd_joins_and_talks_through_the_tunnel`, `zvd_joins_a_distributed_network_through_the_tunnel`, `a_zvd_acting_as_trust_center_attaches_behind_the_trusted_link`, `a_zvd_rejoins_through_the_tunnel` | NPDU Message TLV codec and the trusted-link pre-/post-processing of §7.7.4.1-7.7.4.2 including the ZVD-provisioning-session drop rule of §7.7.3.6.2; the NWK-layer Trusted Link interface (R23.2 NLME-TRUSTEDLINK-PREPROCESSING / POSTPROCESSING, ADR-0017): an NPDU received over the link is processed as from that neighbour without an auxiliary header, treated as NWK-secured when SecurityEnable is set, a joiner heard over the link is kept as a neighbour behind it, and unicasts to it and copies of broadcasts leave as plaintext NPDUs with the SecurityEnable flag telling the peer whether NWK security would have applied. Joining a centralized network on behalf of the ZVD (§7.7.4.3) works end to end in the facade: the Network Commissioning Request (with the Device Capability Extension ZVD flag; dropped without it per §7.7.4.8) over the provisioning session, the response and the Trust Center's Basic authorization key (StandardKeyType 0xB2 under the key-load key, never the network key) back over the tunnel, then frames on the authorized session (a ZDP request answered through the link); a network key update sends a fresh Basic key and the network-key broadcast is declined for the tunnel (§9). The 0xB2 key descriptor layout is an interpretation (ADR-0017 decision 2, requires-clarification). On a distributed network the ZDD router itself derives and transports the Basic key under the distributed global link key (§7.7.4.5). A ZVD operating as Trust Center (§7.7.4.4) attaches behind the link with the Establish Trusted Link commissioning type: accepted NWK-secured from the configured Trust Center for address 0x0000 only, it becomes a router neighbour behind the link exempt from Link Status aging, reported as StackEvent::TrustCenterLinked, and unicasts to 0x0000 leave over the link. ZVD rejoins (§7.7.4.6): a secure rejoin over the authorized session is admitted without a key transport, a Trust Center rejoin over a (limited) provisioning session gets a Basic key derived from the current network key (the policy's verified-unique-key rule is not applied to virtual devices: ADR-0017 decision 6, requires-clarification). A ZVD forming a network (§7.7.4.7) is the out-of-band join of PW-ZD-ZDD-002 on the ZDD side. |
| PW-ZD-ZVD-001 | §8 | mandatory | partially-implemented | `panweave-direct::zvd,panweave-direct::commissioning,panweave-direct::session,panweave-direct::advertisement` | ZVD behaviour: discovery of ZDDs, security, reconnection, commissioning and tunnel clients, EUI-64 allocation | `allocated_identifiers_follow_the_rules`, `the_client_joins_reconnects_and_rejoins`, `discovery_is_retried_at_least_once_then_given_up` | ZVD-side pieces: the advertisement parser (Advertisement::find, §7.6.3), the session Initiator of §6.5 (Curve25519 / P-256, Annex B vectors), the Commissioning Service encoders and the Commissioning Status decoder (commissioning::{FormNetwork, JoinNetwork, ManageJoiners, LeaveNetwork, FindingBinding}::encode, decode_status; §8.4.2), and zvd::TunnelClient, the connection / re-establishment machine of §8.3.3 and the tunnel-client steps of §8.4.3.3 (discovery filtered by the Tunnel Service flag and the ZVD's PAN ID, reconnection to previously known ZDDs by BLE address, at least one discovery retry, an alternative ZDD after a failed session, the Network Commissioning Request kind chosen from the provisioning state and the ZDD's announced key sequence number, address-conflict handling), with zvd::random_eui64 (§8.5, U/L bit set) and zvd::random_short_address (§3.6.1.8). The host supplies the BLE primitives (§8.2 ATT_MTU negotiation is the platform's). Not provided: a ZVD-TS Zigbee stack running over the tunnel (APS / NWK / ZDO / BDB / ZCL on the ZVD, §8.4.3.3); the panweave stack is the ZDD side, and a host may run its own stack on the tunnel client with the codecs of this crate. |
| PW-ZD-KEY-001 | §9 | mandatory | implemented | `panweave-direct::rotation` | Network key rotation with limited authorization sessions | `past_keys_are_kept_and_derive_basic_keys`, `sessions_are_classified`, `transport_key_messages_are_not_tunnelled`, `zdd_keeps_past_network_keys_for_limited_authorization`, `zvd_joins_and_talks_through_the_tunnel`, `annex_b3_vectors`, `a_zdd_on_a_legacy_network_authorizes_the_zvd_itself` | The past-network-key store (PastNetworkKeys, at least one key, persisted by the facade under Kind::DirectPastKeys and recorded on every StackEvent::NetworkKeySwitched), the Basic authorization key derived from a past key (panweave_security::authorization, Annex B.3 vector), the network key sequence number of Session Establishment Message 1 handed to the secret resolver and reported in Established::peer_key_sequence, the Limited Authorization classification (SessionClass: tunnel rules of a provisioning session, no commissioning, no Basic key translation outside it) and the APSME forwarding decision for Transport Key messages conveying a network key are implemented; the ZDD facade applies the decision to every NPDU the NWK layer routes to the ZVD over the Trusted Link, reporting Event::DirectTunnelDeclined for the host to close the connection instead of tunnelling the frame (§9), and the Trust Center delivers a Basic key derived from the prospective network key to every virtual device on a key update (R23.2 §4.6.3.2.2.4, ADR-0017). On a legacy network the Trust Center rejoin of the Limited Authorization session is answered by the ZDD with a Basic key derived from the active network key (§10 item 3, PW-ZD-LEGACY-001). |
| PW-ZD-LEGACY-001 | §10 | conditional | implemented | `panweave-direct::legacy,panweave::direct` | Legacy network support: ephemeral authorization, routing on behalf of ZVD, joining flows per network class | `aps_frames_are_classified`, `the_well_known_key_probe_recognizes_its_own_frames`, `the_ephemeral_session_filters_and_graduates`, `a_zdd_on_a_legacy_network_authorizes_the_zvd_itself` | A ZDD whose §6.2.3 check found a Trust Center that is not Zigbee Direct aware (DirectState::trust_center_aware == Some(false)) applies §10: the network key the Trust Center tunnels never crosses the link; after an initial join the ZVD gets an ephemeral authorization key (global when the tunnelled Transport Key decrypts under ZigBeeAlliance09, unique otherwise: class II / class III), which opens an ephemeral authorization session (§10.1: only APS-secured traffic between the ZVD and 0x0000, unsecured traffic once the Trust Center's key-load secured message was seen, and Relay Message commands pass; NWK commands never), the ZVD is treated as an end-device child for routing on its behalf (§10.1.1) and its unauthenticated-child entry kept alive, and once the Trust Center's data-key secured message follows the ZDD sends the Basic key derived from the active network key on the Trust Center's behalf and makes the ZVD a child; no proof within apsSecurityTimeOutPeriod forgets the ZVD (Event::DirectAuthorizationTimeout). A Trust Center rejoin over a Limited Authorization session is answered with a Basic key derived from the active network key (§10 item 3). Class I (distributed) is §7.7.4.5 (PW-ZD-ZDD-003). The facade test drives class II end to end with a test-side ZVD APS layer against a Trust Center configured with zigbee_direct_aware = false; the unique-ephemeral-key branch (class III) is exercised only by the probe's unit test. Securing the rejoin's Basic key under the well-known key is an interpretation (ADR-0017 decision 8, requires-clarification). |
| PW-ZD-ZCL-001 | §11.3, §6.2.3 | mandatory | implemented | `panweave-zcl::clusters::direct_configuration,panweave::direct` | Zigbee Direct Configuration cluster (0x003D): InterfaceState, AnonymousJoinTimeout, Configure Zigbee Direct Interface / Anonymous Join Timeout, Trust Center authorization, persistence, the Zigbee Direct aware check | `interface_and_timeout_are_configured_by_an_authorized_source`, `the_trust_center_configures_the_zdd_interface` | Server and client with the two attributes and commands (§11.3.5); on a centralized network only APS-secured unicasts from the Trust Center are accepted, on a distributed one any APS-secured unicast, others get Default Response NOT_AUTHORIZED (§11.3.5.1); the facade persists both settings under Kind::DirectConfig, restores them on initialize(), exposes direct_interface_enabled() / anonymous_join_allowed() and restarts the Anonymous Join Countdown Timer on power-up and reconfiguration (§11.3.5.4.4); check_direct_aware() sends the Match_Desc_req of §6.2.3 for the client cluster on the Trust Center. Stopping the BLE advertisements when the interface is disabled is the host's, as is the local stimulus that restarts the countdown. |

## GP1.1.2 — Green Power feature specification, Basic functionality set, v1.1.2

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-GP-FRAME-001 | A.1.4.1 | mandatory | implemented | `panweave-green-power::gpdf` | GPDF: 802.15.4 MAC header, NWK frame control (frame type, protocol version 3, auto-commissioning, NWK FC extension), extended NWK frame control (application id, security level, security key, rx-after-tx, direction), GPD SrcID or IEEE+endpoint, security frame counter, command id, payload, MIC | `parses_the_level_2_test_vector`, `unsecured_and_maintenance_frames`, `ieee_identity_comes_from_the_mac_header` | NWK Frame Control (protocol version 3, frame type, auto-commissioning, extension), Extended NWK Frame Control (ApplicationID 0b000 / 0b010, security level, key, RxAfterTx, direction), SrcID or IEEE + endpoint from the MAC header, frame counter, payload and MIC; the reserved level 0b01, LPED and reserved ApplicationIDs and RxAfterTx + Auto-Commissioning are rejected; parsed against the §A.1.5.4 vectors. |
| PW-GP-FRAME-002 | A.1.4.2 | mandatory | implemented | `panweave-green-power::gpdf` | Frame types: data and maintenance (channel request/configuration) | `unsecured_and_maintenance_frames`, `commissioning_mode_forwards_commissioning_frames` | Data and Maintenance frame types; Maintenance frames carry no addressing, extension or security fields. |
| PW-GP-SEC-001 | A.1.5.3–A.1.5.12, R23.2 Annex H | mandatory | implemented | `panweave-green-power::security` | GP stub security: security levels 0/2/3, nonce construction per application id, key types (shared/individual, derived from GPD key with AES-MMO/CCM), TC-LK protected key transport, frame counter freshness; test vectors | `nonce_vectors`, `level_2_and_3_vectors_from_the_gpd`, `bidirectional_vectors`, `key_derivation_vectors`, `tc_lk_protection_vectors` | CCM* with M = 4 over the GP stub header (levels 0b10 / 0b11, both directions, both ApplicationIDs), the nonce of §A.1.5.3.2, HMAC-MMO derivation of the NWK-key derived group key and of individual keys, TC-LK protection of GPD keys for the Commissioning and Commissioning Reply GPDFs; every vector of §A.1.5.4–§A.1.5.8 is a test. Freshness is enforced by the proxy against the Proxy Table counter. |
| PW-GP-GPD-001 | A.1.6, A.4 | mandatory | implemented | `panweave-green-power::command` | GPD addressing (reserved SrcIDs, application ids 0b000/0b010), bidirectional operation, GPD command ids (switch, sensor, level, color, scenes, commissioning, decommissioning, channel request, success, manufacturer specific) | `gpd_id_ranges`, `commissioning_command_round_trips`, `commissioning_reply_and_channel_commands`, `application_description_round_trips_and_interprets_reports`, `multi_sensor_commissioning_waits_for_the_application_description`, `a_multi_sensor_gpd_reports_through_its_application_description` | GPD identities (SrcID ranges, IEEE + endpoint with the 0x00 / 0xff rules), the GPD command identifiers of Tables 54-56 with the commissioning / to-GPD range helpers, the payload codecs of the commissioning commands (GPD Commissioning with Extended Options, TC-LK protected key + MIC, outgoing counter and Application information incl. cluster list and switch information; Commissioning Reply; Channel Request; Channel Configuration), the Application Description command (report descriptors, data points and attribute records with their offsets and fixed values) with a per-GPD store tolerating out-of-order and duplicate descriptors, the Compact Attribute Reporting command interpreted through the stored descriptor into ZCL attribute values, and the generic translation of the Table 54 / 55 data commands and the attribute reporting commands. The sink pairs a multi-sensor GPD once its Commissioning command and every Application Description are in (handing the Commissioning Reply over on the last one when it opens a receive window) and the runtime turns compact reports into Report Attributes on the paired endpoints. Not decoded: the multi-cluster reporting, ZCL tunneling and bidirectional operation (Request / Read / Write Attributes) payloads, forwarded to the application as raw GPD commands. |
| PW-GP-CL-001 | A.3.3, A.3.4 | mandatory | implemented | `panweave-green-power::cluster` | Green Power cluster 0x0021 server (sink) and client (proxy) attributes and commands: GP Notification, GP Pairing Search, GP Tunneling Stop, GP Commissioning Notification, GP Sink Commissioning Mode, GP Translation Table Update/Request/Response, GP Pairing Configuration, GP Sink Table Request/Response, GP Proxy Table Request/Response, GP Notification Response, GP Pairing, GP Proxy Commissioning Mode, GP Response | `notification_round_trip`, `commissioning_notification_with_failed_security`, `pairing_forms`, `commissioning_mode_and_table_request`, `sink_commissioning_mode_and_response`, `sink_entry_round_trips_in_every_mode`, `table_lookup_follows_the_endpoint_rules`, `pairing_configuration_round_trips`, `pairing_configuration_manages_the_sink_table`, `a_commissioning_tool_configures_a_pairing_on_the_sink` | Client (proxy) and server (sink) attribute identifiers, the Basic Proxy gppFunctionality and Basic Sink gpsFunctionality, gpsSecurityLevel / gpsCommissioningExitMode forms; codecs for GP Notification, GP Commissioning Notification (ADR-0011), GP Pairing, GP Proxy Commissioning Mode, GP Sink Commissioning Mode, GP Response, GP Pairing Configuration (actions, the Sink Table entry body, paired endpoints, application information, report descriptors), GP Proxy / Sink Table Request and Response and the Proxy Table / Sink Table entry OTA formats. The sink applies GP Pairing Configuration per A.3.5.2.4.1 (no action with Send GP Pairing, extend / replace with the gpsSecurityLevel and communication-mode checks and the conflicting-mode refusal, remove a pairing or a group of it, remove GPD, the paired local endpoints handed to the runtime which executes the GPD's commands on them, Device_annce and group membership) and refuses the application description action (no Translation Table). GP Notification Response, GP Pairing Search and GP Tunneling Stop belong to the full-unicast and Proxy Table maintenance functionality marked X for the Basic Proxy / Basic Combo (Tables 21 and 25): not applicable. The Translation Table commands (GP Translation Table Update / Request / Response) are an OPTIONAL sink element and are not implemented (TODO(PW-GP-TRANSLATION-TABLE)); GPD commands are translated by the fixed rules of Tables 54 / 55 instead. |
| PW-GP-PROXY-001 | A.3.2.8, A.3.6.3, A.3.8.1 | mandatory | implemented | `panweave-green-power::proxy` | Proxy basic: proxy table, receive GPDF, duplicate filter, security check per proxy table entry, tunnel as GP Notification (unicast/groupcast/derived group, with alias), commissioning mode entered via GP Proxy Commissioning Mode, forward commissioning frames as GP Commissioning Notification | `unsecured_gpdf_is_tunnelled_to_every_paired_sink`, `secured_gpdf_needs_matching_security_and_freshness`, `commissioning_mode_forwards_commissioning_frames`, `proxy_table_request_by_index_and_by_gpd`, `router_proxy_tunnels_gpdfs_to_the_sink`, `gp_response_appoints_the_selected_sender`, `combo_sink_commissions_a_gpd_through_a_proxy_and_runs_its_commands`, `the_selected_sender_delivers_the_channel_configuration_on_the_gpd_channel` | Basic Proxy: Proxy Table maintained from GP Pairing (add / remove sink, RemoveGPD, level 0b01 refusal, INVALID_FIELD / INSUFFICIENT_SPACE Default Responses for unicast requests), duplicate filter (gpDuplicateTimeout), level / key-type match and freshness check, key recovery per §A.3.7.3.1.4, GP Notification to lightweight unicast sinks (no alias, Dmin), derived groupcast (DGroupID, entry alias, MAC sequence) and commissioned groups (group alias, sequence - 9), commissioning mode with window / exit conditions / unicast originator, GP Commissioning Notification (alias sequence - 12, SecurityProcessingFailed with MIC) including Channel Requests, GP Proxy Table Response, GP Response with the gpTxQueue of the SelectedSender (one entry, transmitted gpTxOffset after the GPD's receive window, FirstToForward). The runtime (feature green-power) feeds protocol-version-3 MAC frames to the stub, registers the Green Power EndPoint, tunnels with NWK/APS aliasing after the delay, transmits the queued GPDFs and persists the Proxy Table. Device_annce for an alias conflict (a Device_annce / Update Device naming a GPD alias for a real device, answered after Dmin + RAND(Dmax), ADR-0016 decision 8) and the SelectedSender's channel switch in commissioning are implemented. gppBlockedGPDID, Proxy Table maintenance (entry status, search counter, GP Pairing Search, passive / active discovery) and full unicast / bidirectional operation in operational mode (GP Notification Response, GP Tunneling Stop) are Advanced Proxy functionality, marked X (not to be implemented) for a Basic Proxy in Table 21: not applicable. |
| PW-GP-SINK-001 | A.3.2.9, A.3.6.2, A.3.8.2 | conditional | implemented | `panweave-green-power::sink` | Sink (combo basic): sink table, commissioning (unidirectional and bidirectional), key derivation/delivery, GP Pairing distribution to proxies, translation of GPD commands to ZCL, security and freshness checks | `unidirectional_commissioning_with_a_protected_oob_key`, `bidirectional_commissioning_hands_out_the_derived_group_key`, `multi_hop_commissioning_through_a_proxy`, `direct_channel_request_and_sink_table_request`, `generic_translation_table`, `combo_sink_commissions_a_gpd_through_a_proxy_and_runs_its_commands` | Sans-I/O Basic Combo sink: Sink Table (ADR-0016), commissioning mode (local request or GP Sink Commissioning Mode, window, exit on first pairing, GP Proxy Commissioning Mode to the proxies), unidirectional and bidirectional commissioning from direct GPDFs and GP Commissioning Notifications (gpsSecurityLevel checks, TC-LK protected OOB keys, key delivery in the Commissioning Reply as gpTxQueue GPDF or GP Response to the proxy, Success verification incl. SecurityProcessingFailed notifications, counter reset rule), Data GPDF with Auto-Commissioning, GP Pairing generation, Decommissioning, operation from direct GPDFs and GP Notifications (duplicate filter, level / key-type match, freshness, wrong-mode corrections), Channel Request answered directly or via GP Response, GP Sink Table Request / Response, generic GPD-to-ZCL translation of Tables 54 / 55. Runtime: Stack::enable_green_power_sink registers endpoint 242 as GP Combo Basic (server attributes of Table 24 read-only), green_power_commission / green_power_stop_commissioning, alias Device_annce, derived / commissioned group membership of endpoint 242, translated commands executed on the paired local endpoints as if from the alias, GreenPowerPaired / Decommissioned / Refused / Command events, Sink Table persisted (Kind::GreenPower ids 0x100+). GP Pairing Configuration is applied per A.3.5.2.4.1 and multi-sensor GPDs are paired once their Application Description is complete, their compact reports translated through it. Not implemented: the Translation Table (generic translation only), Sink Table-based groupcast forwarding, the SinkTable attribute over Read Attributes (use GP Sink Table Request), sink-side channel hopping for direct Channel Requests (ADR-0016); gpTxOffset timing needs hardware validation. |
| PW-GP-COMM-001 | A.3.9 | mandatory | implemented | `panweave-green-power::sink` | Commissioning procedure: sink commissioning mode window, proxy commissioning window, channel request handling, commissioning reply, success | `commissioning_mode_forwards_commissioning_frames`, `router_proxy_tunnels_gpdfs_to_the_sink`, `multi_hop_commissioning_through_a_proxy`, `bidirectional_commissioning_hands_out_the_derived_group_key`, `combo_sink_commissions_a_gpd_through_a_proxy_and_runs_its_commands`, `gp_response_appoints_the_selected_sender`, `bidirectional_commissioning_through_a_proxy_delivers_the_key`, `the_selected_sender_delivers_the_channel_configuration_on_the_gpd_channel` | Proxy side: GP Proxy Commissioning Mode enters / exits the window (default 180 s, overridable), Channel Request and Commissioning / Success / Decommissioning GPDFs are forwarded as GP Commissioning Notifications (broadcast with derived alias or unicast to the originator); GP Response appoints the SelectedSender, whose gpTxQueue answers the GPD's next receive window (RxAfterTx frame or Channel Request without Auto-Commissioning) gpTxOffset later, the others drop their entry (steps 8, 9, 14). Sink side (panweave-green-power::sink): commissioning window with exit modes, GP Proxy Commissioning Mode to the proxies, Channel Request answered with Channel Configuration (directly after gpTxOffset, or GP Response appointing the proxy on the GPD's next channel), Commissioning with the gpsSecurityLevel policy, Commissioning Reply delivered from the sink's gpTxQueue or by GP Response, Success finalising the pairing, GP Pairing and Device_annce. The SelectedSender proxy appointed for another channel switches there for up to 5 s, answers the GPD's Channel Request from its gpTxQueue without forwarding it and returns to the operational channel (steps 8-9; ADR-0016 decision 7); the sink itself never leaves the operational channel and serves GPDs listening there directly. |
| PW-GP-EP-001 | A.2.4, A.2.5 | mandatory | implemented | `panweave-runtime::green_power` | Green Power endpoint 242 with profile 0xA1E0; proxy alias addressing | `router_proxy_tunnels_gpdfs_to_the_sink`, `combo_sink_commissions_a_gpd_through_a_proxy_and_runs_its_commands` | Stack::enable_green_power_proxy registers endpoint 242 (profile 0xA1E0, device GP Proxy Basic 0x0061) with the Green Power cluster client; enable_green_power_sink registers it as GP Combo Basic (0x0066) with server and client. Notifications are sent from it with UseAlias (NWK source / sequence and APS counter = alias, §A.3.6.3.3); the sink's Device_annce on behalf of a GPD uses the alias with NWK sequence and APS counter 0x00 (§A.3.6.3.4.2); aliased broadcasts are keyed by the alias in the broadcast transaction table. |

## SE1.4a — Zigbee Smart Energy Standard 1.4a

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-SE-PROF-001 | §5.2, §5.3 | mandatory | implemented | `panweave-smart-energy::profile` | Stack profile requirements and startup attribute set (join, security, end device, link status, concentrator, APS transport/fragmentation, binding parameters) | `preset_carries_the_table_values`, `profile_preset_is_applied` | Table 5-1 startup, Table 5-2 join, Table 5-4 end-device, Table 5-6 concentrator, Table 5-8 fragmentation and Table 5-9 binding values are constants in panweave-smart-energy::profile, gathered in profile::PRESET; the facade's smart_energy_endpoints::apply_profile sets the ZDO configuration attributes and the poll rate of a StackConfig and apply_profile_to_stack sets the APS inter-frame delay, the node descriptor's maximum incoming transfer size and the concentrator radius of a coordinator. The end-device bind timeout is a constant for an application implementing End_Device_Bind_req on the coordinator. |
| PW-SE-SEC-001 | §5.4.1–§5.4.3 | mandatory | implemented | `panweave-smart-energy::security,panweave::smart_energy_drivers::trust_center` | Joining with preinstalled TCLKs (install codes), rejoin policy, leaving | `registry_applies_key_usage_policies`, `permit_join_broadcasts_are_bounded_and_repeated`, `scan_backoff_grows_to_an_hour_and_resets_on_user_action`, `the_trust_center_provisions_opens_authenticates_and_prunes` | Install-code provisioning (CRC check, MMO-derived preconfigured key with the §5.4.8.1.2 vectors), the Trust Center registry of §5.4.1.1 with join authorization for Trust Center rejoins (§5.4.2.2), the permit-join broadcast rule of §5.4.1.2 (never 255, at most 254 s, repeated every 240 s or on a device announce), the §5.4.1 step 5 scan back-off and the §5.4.3 rule that a leave keeps the link key are implemented as policy objects. The facade SeTrustCenter driver (feature smart-energy) wires them into the runtime Trust Center: provisioning installs the install-code key as the device's provisional unique link key (with the runtime's InstallCodePolicy::Required refusing unprovisioned joiners), permit joining runs the broadcast schedule (immediately, every 240 s and on every Device_annce), DeviceAuthorized / DeviceLeft keep the registry, the Trust Center's CbkeDriver outcome authenticates the device and records its hashed key for the Table 5-10 backup, and devices that never establish a key are removed after the §5.5.5.5 grace period. Joining itself (Annex F key transport under the preconfigured key, Secure / Trust Center rejoin) is the R23 runtime's; the joiner side runs Key Establishment after joining through SeCommissioning / CbkeDriver. |
| PW-SE-SEC-002 | §5.4.4, §5.4.5 | mandatory | implemented | `panweave-smart-energy::security,panweave::smart_energy_drivers::trust_center,panweave-aps` | Network key and link key update policies; key retirement | `registry_applies_key_usage_policies`, `the_trust_center_provisions_opens_authenticates_and_prunes` | Network key update (§5.4.4): the R23 runtime's Stack::update_network_key broadcasts the new key and switches after nwkNetworkBroadcastDeliveryTime; SeTrustCenter::network_key_period runs it periodically with a random key (NetworkKeyUpdated, retried a minute later while another update is in progress). Link key retirement (§5.4.5): SeTrustCenter::link_key_lifetime marks a device's CBKE key stale when it reaches the lifetime (KeyRetired; retire_key does it on demand); the APS layer (Aps::mark_key_stale) then drops data frames secured with that key without an acknowledgement and keeps processing APS commands under it, the stack reports StaleLinkKeyUsed and the driver asks the application to initiate Key Establishment (Renegotiate; the simulator test has the ESI run CbkeDriver::start), a success clearing the stale mark. Retired keys stay installed so a sleepy device can still fetch the network key with them. The lifetime and period are the application's policy (the specification leaves them to the Trust Center). |
| PW-SE-SEC-003 | §5.4.6 | mandatory | requires-clarification | `panweave-smart-energy` | Cluster usage of security keys: APS link-key encryption mandatory for listed clusters | `key_policy`, `unsecured_frames_of_link_key_clusters_are_refused_with_failure` | link_key_required encodes Table 5-12 for every readable row; the rows Power Configuration, Key Establishment and Keep-Alive are unreadable in the source extraction and follow ADR-0012 (all three treated as exempt, Key Establishment necessarily so). Enforcement is wired: Zcl::set_link_key_policy with panweave-smart-energy::zcl_link_key_policy answers an unsecured frame of a cluster that requires the link key with a Default Response FAILURE under the network key and does not process it, accepts secured frames of any cluster and answers them with the same security, and lets the FAILURE Default Response itself through; the simulator test shows all three cases between an IHD and an ESI. The three unreadable rows keep the item partial. |
| PW-SE-SEC-004 | §5.4.7, Annex C | mandatory | implemented | `panweave-smart-energy::key_establishment` | Key Establishment cluster with CBKE suites (ECMQV over sect163k1 suite 1 and sect283k1 suite 2), certificates, test vectors | `annex_c5_suite1_transform`, `annex_c6_suite2_transform`, `annex_c5_exchange_between_machines`, `cbke_over_the_key_establishment_cluster_installs_a_working_link_key`, `registry_applies_key_usage_policies`, `the_driver_runs_cbke_after_joining`, `the_display_leaves_a_trust_center_with_an_unknown_issuer` | Cluster codecs (Initiate / Ephemeral Data / Confirm Key / Terminate, the KeyEstablishmentSuite attribute and suite negotiation, server and client cluster instances), Suite 1 and Suite 2 implicit certificate parsing with the C.3.1.2.3.1.2 checks, the KDF and MACU/MACV transforms (C.5 / C.6 vectors) and the initiator / responder machines are implemented; panweave::cbke::CbkeDriver drives them over the stack on both sides with the C.3.1.1 timeouts and NO_RESOURCES for a second concurrent exchange, starting after a join when asked; the simulator tests run the exchange between a router and the Trust Center and prove the installed link key with an APS-secured read. The ECMQV curve arithmetic over sect163k1 / sect283k1 is supplied by the host through the Ecmqv trait (ADR-0012). The §5.4.7 policies are in panweave-smart-energy::security and applied by the drivers: the joiner leaves on UNKNOWN_ISSUER (SeCommissioning) and retries other failures per the §5.5.5 life cycle, the Trust Center removes devices that do not establish a key within the grace period (SeTrustCenter::removal_after, TRUST_CENTER_REMOVAL_AFTER), refuses data under the preconfigured key, brokers partner keys only between CBKE-authenticated devices and answers a peer Initiate without a partner key with NO_RESOURCES. |
| PW-SE-SEC-005 | §5.4.8, §5.13 | optional | implemented | `panweave-smart-energy::security,panweave-runtime::keep_alive,panweave::smart_energy_drivers::commissioning` | Trust Center swap-out, keep-alive, coexistence rules | `swap_out_procedure_walks_the_candidates`, `link_key_hash_matches_table_5_11`, `router_keep_alive_detects_a_vanished_trust_center`, `the_display_follows_a_trust_center_swap_out` | Keep-alive with three-failure detection is in the runtime (StackEvent::TrustCenterLost); the Table 5-11 link key hash, the Table 5-10 backup records from the registry (and their restore as unauthorized preconfigured keys) and the device-side swap-out procedure of §5.4.2.2.3.5 are implemented as a state machine emitting actions, and the R23 runtime performs the rejoin onto the replacement (Transport Key under the hashed key, apsTrustCenterAddress updated, StackEvent::TrustCenterSwapped). The facade life-cycle driver (SeCommissioning, feature smart-energy) drives it: on TrustCenterLost it rejoins (Trust Center rejoin rounds, then the secured rejoin scan for the extended PAN, then the wait on the original channel), on a detected swap-out it runs Key Establishment with the replacement with one retry before leaving and starting over, and after success rediscovers the ESIs and binds again; the simulator test swaps the ESI for a replacement restored from the hashed backup. Coexistence rules (§5.4.2.2.3.6) are the Trust Center's policy in the registry. |
| PW-SE-COMM-001 | §5.5 | mandatory | implemented | `panweave-smart-energy::commissioning,panweave::smart_energy_drivers::commissioning` | Commissioning: formation, commissioning modes, service discovery and binding | `scan_schedule_follows_5_5_5_1`, `joining_backs_out_and_paces_key_establishment`, `steady_state_recovers_and_returns`, `the_display_joins_establishes_its_key_discovers_the_esi_and_binds` | The §5.5.5 life-cycle is a state machine (Lifecycle): the auto-join scan schedule (immediately, per minute for 15 minutes +-15 s, then hourly +-30 min), backing out of a PAN whose network key cannot be decrypted with the three-per-PAN and ten-before-rescan limits, Key Establishment retries with the fifteen-minute pause after ten failures and no leave, the leave rules of step 7 (APS Remove Device / user always, a parent's NWK leave only for end devices or unauthenticated children), rediscovery periods (3-24 h, after recovery, 60-600 s after a device announce), the 24-hour keep-alive failure limit, and rejoin-and-recovery (three rounds on the channel, scan by extended PAN ID, back to the original channel, hourly +-30 min after four failures, ended by an APS-secured Trust Center message); the ESI rules of §5.5.5.5 (one hour before removing a device without CBKE, five devices minimum) are constants and a predicate. The facade driver panweave::smart_energy_drivers::commissioning::SeCommissioning (feature smart-energy) runs it over a Stack: the scan schedule drives Stack::join (a scan without networks follows the §5.5.5.1 back-off), CBKE runs through CbkeDriver after the join, the ESIs are discovered with a broadcast Match_Desc_req for the device's client clusters on the Smart Energy profile, every discovered ESI is asked (Bind_req, after an IEEE_addr_req when its address is unknown) to bind each of those clusters to the device, steady state rediscovers periodically, TrustCenterLost (three failed keep-alives) enters rejoin and recovery (Trust Center rejoins, then a secured rejoin scan, then the wait on the original channel) and a leave resets the device. Network formation (§5.5.1) is the R23 runtime's. Not done: MDU Pairing before discovery, the APS Remove Device / user leave sources (the application feeds Left events; the driver maps the runtime's NWK leaves). |
| PW-SE-DEV-001 | §6.3 | mandatory | partially-implemented | `panweave-smart-energy::devices` | SE device types (ESI, metering, IHD, PCT, load control, range extender, smart appliance, prepayment terminal, physical device, remote communications) | `device_table_and_checks`, `every_profile_cluster_of_every_device_is_instantiable`, `every_device_builds_and_conforms`, `rules_are_enforced` | Table 5-13 device identifiers with the mandatory / optional server and client clusters of Tables 6-1 and 6-3 to 6-10, the prohibition of unlisted clusters and the at-least-one rules of the In-Home Display and Remote Communications Device, with an endpoint checker; panweave-smart-energy::endpoints instantiates every profile cluster with its mandatory attributes and the facade's smart_energy_endpoints (feature smart-energy) builds each device on profile 0x0109 with the common clusters, checked against the rules. Of the Table 6-2 common features and functions the stack provides joining (end devices and routers), forming (coordinator), factory-fresh reset (Node::factory_reset / Stack::erase_persisted and the Basic Reset to Factory Defaults), permit joining (SeTrustCenter::permit_join), Identify (ZCL Identify server, Zcl::set_identify_time), Match_Desc_req service discovery and its response, ZDP Bind / Unbind responses, Device_annce, and the optional inter-PAN and fragmentation (APS fragmentation) support. Not provided: the Bind Manager (End_Device_Bind_req / rsp, mandatory for the coordinator in Table 6-2) and Pair Devices (End_Device_Bind_req, optional): End_Device_Bind is deprecated in R23.2 §2.4.3.2.1 and the ZDO answers NOT_SUPPORTED (PW-R23-ZDP-020); a coordinator that must serve SE 1.4a legacy pairing needs the application-level binding manager (TODO(PW-SE-END-DEVICE-BIND)). |
| PW-SE-CL-001 | Annex D.2 | mandatory | implemented | `panweave-smart-energy::clusters::drlc,panweave::smart_energy_drivers::drlc` | Demand Response and Load Control cluster | `codecs_round_trip`, `scheduler_accepts_supersedes_and_reports`, `annex_e_overlap_rules`, `server_store_answers_get_scheduled_events`, `load_control_events_are_scheduled_reported_and_cancelled` | Load Control Event / Cancel / Cancel All, Report Event Status (with the 42-octet signature field) and Get Scheduled Events codecs, the client attributes of Table D-7, a client-side Scheduler applying device-class / enrollment-group filtering, expiry rejection, supersede-by-newer-event, start and duration randomization, opt-out, the cancellation rules of D.2.2.3.2 and the Annex E overlap rules, the 0-5 s report delay draw of D.2.3.3.1.2, and a server-side EventStore answering Get Scheduled Events per D.2.3.3.2.3 (completed events excluded, earliest end time and minimum issuer event id filters, id order or start-time order, count limit, per-client device class and enrollment group). The facade drivers (feature smart-energy) bind them to a Stack: DrlcClient takes the events from the stack, syncs the writable client attributes into the scheduler, draws the randomization and report delays from the stack's RNG, sends every Report Event Status to the issuing ESI after its delay (opt-outs and poll-driven starts / completions included) and requests scheduled events; DrlcServer issues, cancels and answers Get Scheduled Events (one Load Control Event per match, NOT_FOUND otherwise) with per-client profiles, reporting the statuses received. All frames go APS link-key protected. The ECDSA signature of Report Event Status is the host's (ADR-0012): reports carry signature type 0. |
| PW-SE-CL-002 | Annex D.3 | mandatory | partially-implemented | `panweave-smart-energy::clusters::metering,panweave::smart_energy_drivers::metering` | Metering cluster | `codecs_round_trip`, `fast_poll_applies_the_limits`, `sampler_answers_get_sampled_data`, `snapshots_are_stored_published_and_reassembled`, `supply_control_applies_change_supply_rules`, `mirror_table_allocates_endpoints`, `profile_log_answers_get_profile`, `meter_answers_profile_fast_poll_sampling_snapshot_supply_and_mirror`, `attribute_set_identifiers_follow_the_tables`, `supply_policy_answers_meter_events` | Mandatory Reading Information, Meter Status and Formatting attributes, the common Historical Consumption attributes, the TOU (per-tier summations 1-48, CPP1/CPP2), Load Profile Configuration, Supply Limit, Block Information delivered / received (per tier and block), Alarms, Meter Billing, Supply Control and Alternative Historical Consumption attribute identifiers, the UnitofMeasure / MeteringDeviceType / Status enumerations, the formatting and multiplier/divisor helpers, the Get Profile / Get Profile Response codecs and a server-side ProfileLog per interval channel (newest-first blocks selected by end time, MaxNumberOfPeriodsDelivered cap, the Table D-48 statuses); clusters::metering::extended adds the codecs of every other command of Tables D-47 / D-63 (mirroring, fast poll, snapshot scheduling / taking / publishing with the TOU sub-payloads, sampling, notification schemes and flags, supply control) and the server state behind them: FastPoll with the D.3.4.2 limits, a Sampler answering GetSampledData, a Snapshots store answering GetSnapshot with offsets and fragmenting Publish Snapshot (plus a client SnapshotAssembler), SupplyControl applying Change Supply / Local Change Supply with the immediate / delayed / cancel / NOT_AUTHORIZED / UNSUP rules, SetSupplyStatus (the required status after tamper, depletion, uncontrolled flow and load limit, newer issuer event ids only), SetUncontrolledFlowThreshold (zero multiplier / divisor refused) and ResetLoadLimitCounter, with meter events applying the policy and counting load-limit events, and an ESI MirrorTable allocating mirror endpoints. The facade drivers (feature smart-energy) bind them to a Stack: MeteringServer answers Get Profile, Request Fast Poll Mode, Start Sampling / Get Sampled Data, Take Snapshot / Get Snapshot, Change Supply / Local Change Supply / Set Supply Status / Set Uncontrolled Flow Threshold / Reset Load Limit Counter (the policy mirrored into the Supply Limit and Supply Control attributes when present), and sends Request / Remove / Configure Mirror; MeteringClient issues the requests, reports the responses, assembles snapshots and hosts mirrors. All frames go APS link-key protected. Not done: the block-tier snapshot sub-payloads, snapshot schedules triggered by the server (Schedule Snapshot is refused as unsupported), the notification-flag predefined schemes A / B, and the meter's own load-limit / uncontrolled-flow detection from CurrentDemandDelivered (the application raises SupplyEvent). |
| PW-SE-CL-003 | Annex D.4 | mandatory | implemented | `panweave-smart-energy::clusters::price,panweave::smart_energy_drivers::price` | Price cluster | `codecs_round_trip`, `scheduled_store_keeps_current_and_next`, `tariff_store_assembles_and_cancels`, `server_schedule_answers_current_and_scheduled`, `prices_are_published_acknowledged_and_queried` | Publish Price with every optional field (SE 1.0 short frames accepted), Get Current Price, Get Scheduled Prices and Price Acknowledgement codecs, a client-side PriceTable applying the overlap rules of D.4.2.4.1 and a server-side PriceSchedule (D.4.2.3.2 / D.4.2.3.3: the price in force, the prices ending at or after the requested start time in start order with the count limit, acknowledgements per client, expiry); clusters::price::extended adds the codecs of every optional command (block periods, conversion factor, calorific value, tariff information, price matrix and block thresholds with fragmentation, CO2 value, tier labels, billing periods, consolidated bills, CPP events and responses, credit payments, currency conversion, cancel tariff, the Get* requests), a generic Scheduled store keeping the current and next instance per tariff type with the 0xFFFFFFFF cancellation, newer-issuer-event and Get* ordering rules, and a TariffStore assembling a tariff from its fragmented parts with price lookup, block-for-consumption and CancelTariff. The facade drivers (feature smart-energy) bind the core to a Stack: PriceClient stores published prices, answers with Price Acknowledgement when required, expires prices and requests the current / scheduled prices; PriceServer publishes and answers Get Current Price (one Publish Price or NOT_FOUND), Get Scheduled Prices (one Publish Price per match) and records acknowledgements. All frames go APS link-key protected. Not done: server-side answering of the optional Get* requests (block periods, tariffs, bills ...) beyond Scheduled::scheduled, and the provisional Price client attributes. |
| PW-SE-CL-004 | Annex D.5 | mandatory | implemented | `panweave-smart-energy::clusters::messaging,panweave::smart_energy_drivers::messaging` | Messaging cluster | `codecs_and_display`, `server_store_answers_get_last_message_and_cancellation`, `esi_messages_reach_the_display_and_are_confirmed` | Display Message / Display Protected Message / Cancel Message / Cancel All Messages and Get Last Message / Message Confirmation / GetMessageCancellation codecs (inter-PAN-only messages refused per D.5.2.3.1.1.1), a client-side Display holding the single current message with the confirmation-status and enhanced-confirmation rules, and a server-side Server holding the current message for Get Last Message, gathering Message Confirmations per client, and keeping a pending Cancel All Messages for GetMessageCancellation until its implementation time. The facade drivers (feature smart-energy) bind them to a Stack: MessagingClient decodes the server's commands from the stack events, applies them to the Display, answers with Default Responses (MALFORMED_COMMAND, INVALID_FIELD, NOT_FOUND, INSUFFICIENT_SPACE), expires messages and scheduled cancellations on poll, and sends Get Last Message / Message Confirmation / GetMessageCancellation; MessagingServer publishes, cancels and answers Get Last Message (Display (Protected) Message or NOT_FOUND) and GetMessageCancellation, recording confirmations. All frames go APS link-key protected (Table 5-12). UTC comes from the endpoint's Time server or the driver's UtcClock. |
| PW-SE-CL-005 | Annex D.6 | optional | implemented | `panweave-smart-energy::clusters::tunneling` | Tunneling cluster | `request_close_and_status_codes`, `data_transfer_errors_and_flow_control`, `idle_tunnels_close_with_a_notification`, `supported_protocols_pages` | All D.6 commands as codecs (RequestTunnel / Response, CloseTunnel, TransferData both ways, TransferDataError, AckTransferData, ReadyData, Get Supported Tunnel Protocols / Response with 16-entry paging, TunnelClosureNotification) and a bounded server tunnel table: tunnel identifiers never reused while open, tunnels bound to the requesting device (WRONG_DEVICE otherwise), MaximumIncomingTransferSize overflow detection, the TunnelStatus codes (protocol / flow control not supported, no more identifiers), CloseTunnel by the owner only with NOT_FOUND otherwise, flow-control windows honoured by the server's own transfers, and the CloseTunnelTimeout inactivity closure with notification. The table is driven by the application (SE endpoint dispatch is not wired into the ZCL dispatcher); APS fragmentation for large transfers is the runtime's existing fragmentation. |
| PW-SE-CL-006 | Annex D.7 | optional | partially-implemented | `panweave-smart-energy::clusters::prepayment` | Prepayment cluster | `command_codecs_round_trip`, `response_codecs_round_trip`, `account_applies_top_ups_limits_and_status`, `account_tracks_debts`, `timed_commands_are_scheduled_cancelled_and_applied` | Attribute identifiers of the information, top-up (#1–#5), debt (#1–#3) and alarm sets with the PaymentControlConfiguration, CreditStatus and PrepaymentAlarmStatus bitmaps; codecs for all twelve client commands and the five server commands; an Account applying the D.7 credit rules (top-up validation and limits, emergency credit, credit adjustment, debt changes and collections with the logs, CreditStatus per D.7.4.1); and a Pending scheduler for the timed commands (Emergency Credit Setup, Credit Adjustment, Change Payment Mode, Set Maximum Credit Limit, Set Overall Debt Cap): 0 applies now, 0xFFFFFFFF cancels by issuer event id and provider, a newer pending command replaces an older one of the same kind, due commands apply on poll. Not done: the historical cost consumption set, friendly credit periods and calendars, top-up token (UTRN) validation (the application decides the amount), the alarm mask groups and the Mirror NOTIFICATION_PENDING behaviour (TODO(PW-SE-PREPAY-CALENDAR)). |
| PW-SE-CL-007 | Annex D.9, D.10, D.11, D.12, D.13, D.14 | optional | implemented | `panweave-smart-energy::clusters::calendar` | Calendar, Device Management, Events, Energy Management, MDU Pairing, Sub-GHz clusters | `codecs_round_trip`, `store_resolves_the_day_profile_in_force`, `special_day_tables_replace_and_cancel`, `log_pages_most_recent_first_and_clears_where_allowed`, `manage_event_applies_opt_out_rules`, `duty_time_algorithm`, `server_fragments_and_client_reassembles`, `suspension_holds_traffic_for_the_period`, `event_configuration_selectors_and_reports`, `pending_changes_apply_and_cancel` | Calendar (D.9), Device Management (D.10), Events (D.11), Energy Management (D.12, provisional), MDU Pairing (D.13, provisional) and Sub-GHz (D.14) codecs with client/server state helpers, instantiable on Smart Energy endpoints through panweave-smart-energy::endpoints. Sub-GHz radio operation itself needs a sub-GHz PHY (requires hardware validation); command dispatch to the state helpers is the application's. |
| PW-SE-CL-008 | Annex A.3, §6.1.6 | mandatory | implemented | `panweave-zcl::clusters::keep_alive,panweave-runtime::keep_alive,panweave::smart_energy_drivers::commissioning` | Keep-Alive cluster | `router_keep_alive_detects_a_vanished_trust_center`, `the_display_follows_a_trust_center_swap_out` | The ZCL8 §3.18 cluster (TCKeepAliveBase / TCKeepAliveJitter server, router client behaviour with three-failure detection) is implemented in the runtime; the Smart Energy reaction to a lost Trust Center — rejoin and recovery and the swap-out procedure with Key Establishment against the replacement — is driven by the facade SeCommissioning driver. |
| PW-SE-CL-009 | Annex B | optional | requires-hardware-validation | `panweave-smart-energy::interpan` | Enhanced inter-PAN transmission | `security_policy_follows_annex_b`, `survey_keeps_the_best_beacon_per_device`, `secured_frames_round_trip_between_link_key_holders` | The stub APS header carries the Security / ACK Request (APS counter) / Extended Header bits of Figure B-7, group delivery uses 0b11, and panweave-aps::interpan::secure / unsecure protect a frame with the APS link key of the partner (extended nonce, replay refused). The SE policy of B.4 / B.8 (every cluster secured except Key Establishment, non-conforming frames dropped) and the B.6 channel-survey candidate list are in panweave-smart-energy::interpan. The exempt cluster is taken as 0x0800 and the authenticated data starts at the APS frame control (ADR-0013); both await a captured frame from another implementation. The runtime dispatches only touchlink inter-PAN frames; SE clusters over inter-PAN are driven by the application through the MAC service (TODO(PW-SE-INTERPAN-RUNTIME)). |
| PW-SE-EV-001 | Annex E | mandatory | implemented | `panweave-smart-energy::clusters::drlc` | Overlapping DRLC event rules | `annex_e_overlap_rules`, `scheduler_accepts_supersedes_and_reports` | Scheduler applies rules 3 (expired events rejected 0xFB), 4 (start in the past runs from now with the original end), 5a/5b (newer overlapping events supersede; a running event keeps its state until the successor's effective start and switches directly, reported Superseded once), 6b/6d (overlap judged on scheduled periods so randomization never conflicts; a successive event's effective start truncates or extends the predecessor's effective end, no artificial gap) and 8 (supersession only when the new event covers every device class of the previous one that this device has). Rules 1, 2 and 7 bind the ESI / upstream systems and need no client code. |

## ZCL8 — Zigbee Cluster Library Specification, Revision 8

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-ZCL-FND-001 | §2.4.1 | mandatory | implemented | `panweave-zcl::frame` | ZCL frame header: frame control (frame type, manufacturer specific, direction, disable default response), optional manufacturer code, transaction sequence number, command identifier | `header_round_trip` |  |
| PW-ZCL-FND-002 | §2.6.2 | mandatory | implemented | `panweave-zcl::types` | Data types: null, general data, boolean, bitmaps, unsigned/signed integers 8–64 bit, enums, floats, strings (octet/character, long variants), arrays/structures/sets/bags, time/date/UTC, cluster/attribute/BACnet OID, IEEE address, 128-bit security key; invalid/unknown values | `type_ids_round_trip`, `value_round_trips`, `semi_precision_converts_both_ways`, `composite_elements_iterate_and_build` | All type identifiers, fixed-size types, strings and composite length walking; array / structure / set / bag values carry their raw encoding with element iteration (Value::elements), builders (write_array / write_struct) and the element selectors of the structured commands; semi-precision floats convert to and from single precision (IEEE 754 binary16, round to nearest even) and compare as magnitudes for reporting. |
| PW-ZCL-FND-003 | §2.5.1, §2.5.2 | mandatory | implemented | `panweave-zcl::cluster` | Read Attributes / Read Attributes Response with per-attribute status | `read_and_write_records`, `read_write_and_discover_attributes` |  |
| PW-ZCL-FND-004 | §2.5.3–§2.5.6 | mandatory | implemented | `panweave-zcl::cluster` | Write Attributes, Write Attributes Undivided, Write Attributes Response, Write Attributes No Response; type checking, read-only, invalid value semantics | `read_and_write_records`, `read_write_and_discover_attributes` | Range checks beyond the data type (step 5 of §2.5.3.3) are cluster-specific and left to the application. |
| PW-ZCL-FND-005 | §2.5.7, §2.5.8 | mandatory | implemented | `panweave-zcl::cluster` | Configure Reporting / Response with min/max intervals, reportable change, timeout; direction semantics | `reporting_records`, `reporting_configuration_and_reports` |  |
| PW-ZCL-FND-006 | §2.5.9, §2.5.10 | mandatory | implemented | `panweave-zcl::cluster` | Read Reporting Configuration / Response | `reporting_records`, `reporting_configuration_and_reports` |  |
| PW-ZCL-FND-007 | §2.5.11 | mandatory | implemented | `panweave-zcl::attribute` | Report Attributes command generated by the reporting engine according to configuration | `reporting_schedule`, `reporting_configuration_and_reports` | Reports are addressed via the binding table (Destination::Bound); timer consolidation of §2.5.11.2.5 is not applied (one deadline per attribute). |
| PW-ZCL-FND-008 | §2.5.12 | mandatory | implemented | `panweave-zcl::layer` | Default Response generation rules (unicast, not for default responses, disable-default-response bit, error cases) | `default_responses_and_cluster_specific_commands` |  |
| PW-ZCL-FND-009 | §2.5.13, §2.5.14 | mandatory | implemented | `panweave-zcl::cluster` | Discover Attributes / Response with discovery complete flag | `discovery_and_default_response`, `read_write_and_discover_attributes` |  |
| PW-ZCL-FND-010 | §2.5.15–§2.5.18 | optional | implemented | `panweave-zcl::structured` | Read/Write Attributes Structured | `selectors_round_trip_and_reject_depth`, `select_elements_and_counts`, `replace_elements_and_resize_arrays`, `sets_and_bags_add_and_remove`, `read_structured_selects_elements_and_counts`, `write_structured_replaces_adds_and_removes` | Selectors (indicator nibbles, depth limited to 4 as the application limit §2.5.15.1.3 allows), element reads from arrays and structures with index 0 as the count, whole-attribute reads and writes through the plain rules, element replacement with INVALID_DATA_TYPE / INVALID_SELECTOR / READ_ONLY, set and bag add (DUPLICATE_EXISTS for sets) and remove (NOT_FOUND), the Write Attributes Structured Response with the failing attribute and selector and the lone SUCCESS record. Array lengths (index 0 writes) are read-only in the dispatcher: the content of new elements is application dependent and no fill hook is exposed (structured::replace accepts one). |
| PW-ZCL-FND-011 | §2.5.19–§2.5.22 | mandatory | implemented | `panweave-zcl::cluster` | Discover Commands Received/Generated and Discover Attributes Extended with responses | `discovery_and_default_response`, `read_write_and_discover_attributes` |  |
| PW-ZCL-FND-012 | §2.6.3 | mandatory | implemented | `panweave-zcl::frame` | Status enumeration | `status_round_trip` |  |
| PW-ZCL-FND-013 | §2.3.3, §2.4.2 | mandatory | implemented | `panweave-zcl::layer` | Manufacturer-specific clusters, attributes, commands; command direction and server/client roles; unsupported cluster/attribute/command handling | `default_responses_and_cluster_specific_commands`, `group_delivery_targets_member_endpoints_only`, `manufacturer_specific_clusters_need_their_code` | Direction / role dispatch, UNSUPPORTED_CLUSTER and UNSUP_GENERAL_COMMAND; manufacturer-specific attributes and commands of standard clusters carry the code per attribute / command; manufacturer-specific clusters are registered with ClusterInstance::manufacturer_specific and addressed only by frames carrying their code (UNSUPPORTED_CLUSTER without one, UNSUP_MANUF_GENERAL_COMMAND / UNSUP_MANUF_CLUSTER_COMMAND with another), the responses echoing the code. |
| PW-ZCL-FND-014 | §2.3.2 | mandatory | implemented | `panweave-zcl::cluster` | Global attributes: ClusterRevision, AttributeReportingStatus | `multi_frame_reports_carry_attribute_reporting_status` | ClusterRevision is added to every instance; when a report does not fit one Report Attributes frame the last record of each frame is AttributeReportingStatus (Pending in all but the final frame, which carries Complete); single-frame reports carry no status. |
| PW-ZCL-GEN-001 | §3.2 | mandatory | implemented | `panweave-zcl::clusters::basic` | Basic cluster attributes and Reset to Factory Defaults command | `read_write_and_discover_attributes`, `reset_to_factory_defaults_restores_attributes_and_reporting` | Reset to Factory Defaults is executed by the endpoint dispatcher: every attribute of every cluster on every endpoint returns to its initial value and default reporting configuration, transient cluster state is cleared, and the application is told with ZclEvent::FactoryReset; network state, bindings and groups are untouched (§3.2.2.3.1). |
| PW-ZCL-GEN-002 | §3.3 | optional | implemented | `panweave-zcl::clusters::power_configuration` | Power Configuration cluster | `battery_thresholds_drive_alarm_state_and_codes`, `mains_dwell_delays_the_alarm`, `battery_sources_two_and_three_have_their_own_sets`, `battery_alarm_reaches_the_bound_client_with_a_time_stamp` | Mains and battery attributes for sources 1-3 (information, settings, thresholds; Table 3-27 layout through battery_attr / add_battery_source), BatteryAlarmState with reporting and per-source bits, the alarm masks and codes, threshold evaluation into BatteryAlarmState bits and Alarms codes, mains voltage threshold checks with the MainsVoltageDwellTripPoint timer run by the dispatcher (mains_tick raises through the Alarms server) and mains-lost handling. Readings come from the application (set_battery_source, set_mains_voltage, set_mains_available), which raises the battery alarms it is handed. |
| PW-ZCL-GEN-003 | §3.5 | mandatory | implemented | `panweave-zcl::clusters::identify` | Identify cluster: IdentifyTime attribute, Identify, Identify Query, Trigger Effect commands, Identify Query Response | `identify_server_countdown_and_query`, `bdb_formation_steering_finding_binding_and_rejoin` | Server executed by the endpoint dispatcher: one-second countdown, Identify Query answered only while identifying, Trigger Effect surfaced to the application as an event (effect execution is application-defined). |
| PW-ZCL-GEN-004 | §3.6 | mandatory | implemented | `panweave-zcl::clusters::groups` | Groups cluster: NameSupport, Add/View/Get Membership/Remove/Remove All/Add If Identifying and responses | `bdb_formation_steering_finding_binding_and_rejoin` | Server commands operate on apsGroupTable through GroupStore; group names are not supported (NameSupport bit 7 clear, View Group returns the null string); groupcast/broadcast deliveries generate no response (§3.6.2.3.1). |
| PW-ZCL-GEN-005 | §3.7 | optional | implemented | `panweave-zcl::clusters::scenes` | Scenes cluster with scene table, extension fields, transitions | `scenes_store_recall_and_the_global_scene` | Scene table of 16 entries per endpoint with the On/Off and Level Control extension field sets; Add/View/Remove/Remove All/Store/Recall/Get Scene Membership, Enhanced Add/View (tenths) and Copy Scene with the §3.7.2.4 status rules; groupcast/broadcast deliveries generate no response; scene names are not supported (NameSupport bit 7 clear); recall applies the field sets over the transition time through the Level Control engine; SceneValid tracks On/Off and Level changes; the global scene (0/0) backs Off With Effect / On With Recall Global Scene. LastConfiguredBy is not kept. |
| PW-ZCL-GEN-006 | §3.8 | mandatory | implemented | `panweave-zcl::clusters::on_off` | On/Off cluster: OnOff, GlobalSceneControl, OnTime, OffWaitTime, StartUpOnOff; Off/On/Toggle/Off With Effect/On With Recall Global Scene/On With Timed Off | `default_responses_and_cluster_specific_commands`, `on_with_timed_off_counts_down_and_guards_the_off_state`, `scenes_store_recall_and_the_global_scene` | Executed by the endpoint dispatcher: Off/On/Toggle with the OnTime/OffWaitTime rules, Off With Effect (global scene stored, effect surfaced as ZclEvent::OffWithEffect), On With Recall Global Scene, On With Timed Off with the 100 ms countdown read literally from §3.8.2.3.6.4 (an expired OnTime clears OffWaitTime; an Off command during the timed-on period keeps the guard), GlobalSceneControl per §3.8.2.2.2, StartUpOnOff applied by on_off::start_up when the application supplies the previous value (attributes are not persisted by the stack). OnOff carries the BDB default reporting configuration. |
| PW-ZCL-GEN-007 | §3.10 | mandatory | implemented | `panweave-zcl::clusters::level` | Level Control cluster: CurrentLevel and related attributes; Move to Level, Move, Step, Stop (with/without On/Off), transitions and rate semantics | `level_transitions_run_on_the_tick_and_couple_with_on_off`, `scenes_store_recall_and_the_global_scene` | Move to Level / Move / Step / Stop and the with-On/Off variants with OptionsMask/OptionsOverride and ExecuteIfOff processing; linear transitions driven by the dispatcher's 100 ms tick with RemainingTime; Rate 0xff uses DefaultMoveRate; clamped steps shorten proportionally; with-On/Off commands set OnOff (and GlobalSceneControl) per §3.10.2.3.6; Table 3-55 On/Off fades with OnLevel/On-/OffTransitionTime and restoration of the stored level (applied on a change of OnOff only); CurrentFrequency / Move to Closest Frequency are not implemented; StartUpCurrentLevel is applied by level::start_up on the application's request. |
| PW-ZCL-GEN-008 | §3.11 | optional | implemented | `panweave-zcl::clusters::alarms` | Alarms cluster | `table_and_commands`, `battery_alarm_reaches_the_bound_client_with_a_time_stamp` | Alarm table (oldest replaced when full), AlarmCount, Reset Alarm / Reset All Alarms (handed to the application as ZclEvent::AlarmReset), Get Alarm / Get Alarm Response and Reset Alarm Log executed by the dispatcher; Zcl::raise_alarm logs and sends the Alarm command to bound clients, time-stamped from a Time server on the same endpoint. |
| PW-ZCL-GEN-009 | §3.12 | optional | implemented | `panweave-zcl::clusters::time` | Time cluster | `clock_runs_and_guards_writes`, `battery_alarm_reaches_the_bound_client_with_a_time_stamp` | Time, TimeStatus (fixed Master / MasterZoneDst bits, Synchronized cleared on a master), TimeZone, DstStart / DstEnd / DstShift, StandardTime, LocalTime, LastSetTime and ValidUntilTime; the clock runs on the dispatcher's monotonic time, a network write of Time is refused on a master clock (READ_ONLY) and otherwise applied as the new base with ZclEvent::TimeSet; the zone attributes are read-only under MasterZoneDst. The client-side server selection rules of §3.12.2.2.2 are documented, not automated. |
| PW-ZCL-GEN-010 | §3.15 | optional | implemented | `panweave-zcl::clusters::diagnostics` | Diagnostics cluster | `ias_zone_auto_enroll_request_and_status_notification` | Every attribute of section 3.15.2.2 is instantiated by diagnostics::server and Stack::refresh_diagnostics fills it from the stack counters: MAC broadcast / unicast rx and tx, retries, failures and transmit queue overflows (MacService::stats), APS broadcast / unicast rx and tx, successes, retries, failures and frame counter failures, NWK frame counter and decryption failures, unauthorized keys, buffer allocation failures, relayed unicasts, validation drops, route discoveries initiated, neighbor table additions / removals / stale entries, join indications, children moved, the average MAC retries per APS message and the LQI / RSSI of the last frame. NumberOfResets and PersistentMemoryWrites are supplied by the application through Counters. |
| PW-ZCL-GEN-011 | §3.16 | optional | implemented | `panweave-zcl::clusters::poll_control` | Poll Control cluster: check-in, fast polling, long/short poll intervals | `poll_control_server_checks_in_and_fast_polls`, `poll_control_check_in_puts_the_sleepy_device_into_fast_poll_mode` | Server: Table 3-132 attributes with the CheckInInterval/FastPollTimeout write validation, Check-in to the bound clients at a constant CheckInInterval (§3.16.6.1, re-armed when the attribute changes), temporary fast poll mode for 7.68 s awaiting responses, Check-in Response (longest timeout wins, INVALID_FIELD above FastPollTimeoutMax, FAILURE when late), Fast Poll Stop, Set Long/Short Poll Interval with the §3.16.4.2 relationships; the runtime switches the MAC poll rate on ZclEvent::FastPoll / LongPollInterval. Client: answers Check-ins with a configured policy and raises ZclEvent::CheckIn. The 'unbound client → FAILURE' SHOULD is not enforced (the dispatcher does not see the binding table). |
| PW-ZCL-GEN-012 | §3.4 | optional | implemented | `panweave-zcl::clusters::configuration` | Device Temperature Configuration cluster | `device_temperature_alarms_after_the_dwell` | Information and settings sets, extremes tracked from readings, LowTempThreshold / HighTempThreshold consistency and range guards, the mask, and the Low / High dwell trip points run as timers by the dispatcher, raising alarm codes 0x00 / 0x01 through the endpoint Alarms server. OverTempTotalDwell is the application's to accumulate. |
| PW-ZCL-GEN-013 | §3.9 | optional | implemented | `panweave-zcl::clusters::configuration` | On/Off Switch Configuration cluster | `switch_configuration_maps_actions_to_commands` | SwitchType and SwitchActions with the Table 3-54 mapping to On / Off / Toggle (command_for); the switch state transitions themselves come from the application. |
| PW-ZCL-GEN-014 | §3.13 | optional | implemented | `panweave-zcl::clusters::rssi_location` | RSSI Location cluster | `codecs_round_trip`, `server_answers_and_schedules`, `location_requests_are_answered_and_repeated`, `pings_rssi_reports_and_anchors` | Location Information (LocationType bit fields, LocationMethod, LocationAge, QualityMeasure, NumberOfDevices) and Location Settings (Coordinate1-3, Power, PathLossExponent, ReportingPeriod, CalculationPeriod, NumberRSSIMeasurements) attributes; codecs for the seven received and eight generated commands including the conditional fields of the location data (Coordinate 3 for 3-D, the calculation fields for measured locations, the compact form). Server behaviour in the dispatcher: Set Absolute Location and Set Device Configuration update the attributes (the latter refused with FAILURE for an absolute location, §3.13.2.3.2 names no status); Get Device Configuration and Get Location Data are answered for the device's own IEEE address (the instance state) and NOT_FOUND for others, a broadcast asking for absolute locations from a measured device is not answered, Number Responses > 1 sends the follow-ups as Location Data / Compact Location Data Notifications every ReportingPeriod (1 s when 0), unicast to the requester or broadcast; Send Pings blasts NumberRSSIMeasurements RSSI Pings CalculationPeriod apart; RSSI Responses are collected per neighbour and reported to the bound central device as Report RSSI Measurements after CalculationPeriod; ReportingPeriod > 0 sends periodic Location Data Notifications to the bound clients; Anchor Node Announce and a recalculate request reach the application. Not done: the recommended broadcast radius 1 for pings and notifications (the ZCL layer has no per-frame radius; TODO(PW-ZCL-RSSI-RADIUS)) and locations of third devices (Get Location Data for another address answers NOT_FOUND). The client side is codecs only (the application performs the localization algorithm). |
| PW-ZCL-GEN-015 | §3.14 | optional | implemented | `panweave-zcl::clusters::io` | Analog / Binary / Multistate Input, Output and Value clusters | `analog_input_is_writable_out_of_service_and_flags_follow`, `binary_output_and_multistate_value` | All nine clusters (0x000c–0x0014) as servers of one Kind: OutOfService, PresentValue (single / bool / uint16, reported), StatusFlags (reported; Fault from Reliability, Out Of Service from OutOfService, Overridden from the application), Reliability, Description; EngineeringUnits, Min / MaxPresentValue and Resolution for analog kinds, Polarity and the active / inactive texts for binary kinds, NumberOfStates for multistate kinds, RelinquishDefault and the minimum on / off times for outputs and values. An input PresentValue is writable over the air only while out of service (NOT_AUTHORIZED otherwise) and ignores physical readings then. PriorityArray and StateText (arrays) are not instantiated: a PresentValue write is the priority-16 write; ApplicationType is defined but not instantiated; the In Alarm flag stays clear (no EventState cluster). |
| PW-ZCL-GEN-016 | §3.17 | optional | implemented | `panweave-zcl::clusters::power_profile` | Power Profile cluster | `codecs_round_trip`, `server_answers_requests_and_applies_schedules`, `power_profile_is_forecast_scheduled_and_priced` | TotalProfileNum, MultipleScheduling, EnergyFormatting, EnergyRemote and ScheduleMode (writable, reportable); codecs for every §3.17.5 / §3.17.6 command (Power Profile Notification / Response with up to 7 energy phases per frame, State Response / Notification records, Get Power Profile Price (Extended) and Overall Schedule Price with their responses, Energy Phases Schedule Notification / Response / Request / State Response / State Notification, Schedule Constraints Notification / Response / Request). The server keeps up to four profiles (forecast, current record, accepted schedule, constraints) in the cluster instance; the dispatcher answers the four requests (NOT_FOUND for an unknown profile, all profiles for ID 0), applies received schedules (NOT_AUTHORIZED when the profile is not remotely controllable or a phase has MaxActivationDelay 0, NOT_FOUND for an unknown phase, PROGRAMMED → ENERGY_PHASE_WAITING_TO_START, Energy Phases Schedule State Notification when the schedule changed) and hands prices to the application (ZclEvent::PowerProfilePrice / PowerProfileOverallPrice). The application sets forecasts, states, remote control and constraints and asks for schedules and prices through the Zcl::power_profile_* helpers; the client side (scheduler) is codecs only. Profiles longer than 7 phases need the Partition cluster (not implemented). |
| PW-ZCL-GEN-017 | §3.19, §3.20 | optional | implemented | `panweave-zcl::clusters::level` | Level Control for Lighting and Pulse Width Modulation clusters | `frequency_lighting_and_pwm`, `level_couples_the_colour_temperature_and_pwm_runs_beside_it` | Level Control for Lighting: lighting_server enforces the 1…0xfe level range; the CoupleColorTempToLevel Options bit couples CurrentLevel to ColorTemperatureMireds in colour temperature mode (full level = CoupleColorTempToLevelMinMireds, minimum level = ColorTempPhysicalMaxMireds, linear between) through the dispatcher. The frequency attributes and Move to Closest Frequency (approximated by clamping into MinFrequency…MaxFrequency; INVALID_VALUE beyond half / double the range or zero) on the base cluster; Pulse Width Modulation (0x001c) as pwm_server over the same transition engine with a 0…100 % duty cycle, its own commands and ZclEvent::DutyCycle / Frequency. Not done: the Level Control couplings of §3.19.4 beyond Table 3-55 are examples only; StateText-style semantics do not apply. |
| PW-ZCL-MEAS-001 | §4.2–§4.8 | optional | implemented | `panweave-zcl::clusters::measurement` | Measurement clusters: Illuminance, Illuminance Level Sensing, Temperature, Pressure, Flow, Water Content (humidity), Occupancy Sensing | `servers_hold_and_report_readings`, `temperature_sensor_reports_through_its_default_configuration` | Attribute-only servers with the §4.1.3 range attributes, unknown / invalid markers, default reporting of the measured value (BDB 3.1 §6.5) and application setters: Illuminance Measurement, Illuminance Level Sensing, Temperature, Pressure (incl. the extended scaled set definitions), Flow, Water Content (Relative Humidity / Leaf Wetness / Soil Moisture) and Occupancy Sensing with the sensor-type alignment of Table 4-24; facade endpoints for the Light, Occupancy and Temperature Sensor devices. The occupancy delay / threshold attributes are defined but not instantiated by default. |
| PW-ZCL-MEAS-002 | §4.9 | optional | implemented | `panweave-zcl::clusters::electrical_measurement` | Electrical Measurement cluster | `dc_server_tracks_extremes_and_alarms`, `ac_server_readings_and_alarms`, `profile_codecs_round_trip`, `polyphase_totals_harmonics_and_voltage_quality` | MeasurementType, the DC set (readings, minima / maxima, formatting, overload mask and thresholds) and the single-phase / phase A AC set (frequency, RMS voltage with extremes, RMS current and active power with extremes once enabled, reactive / apparent power, power factor, formatting, ACAlarmsMask with the voltage / current / active / reactive power overload thresholds and the average / extreme over and under voltage, sag and swell thresholds with the over / under voltage counters and period attributes), the AC non-phase-specific set (frequency extremes, neutral current, polyphase totals, harmonic and phase harmonic currents with their multipliers) and the phase B / C sets over the phase A layout (Tables 4-39 / 4-40); readings fed by the application with the alarm codes (the mask bit numbers) to raise through the Alarms cluster; Get Profile Info / Get Measurement Profile request and response codecs, the profile itself left to the application. The runtime's 36-attribute instances hold one phase with the overloads; polyphase servers need a larger attribute capacity. Averaging over AverageRMSVoltageMeasurementPeriod and the period attributes are the application's to maintain. |
| PW-ZCL-MEAS-003 | §4.10–§4.13 | optional | implemented | `panweave-zcl::clusters::measurement` | Electrical Conductivity, pH, Wind Speed and Concentration Measurement clusters | `scalar_and_concentration_servers` | Electrical Conductivity (0x040a, 10 x mS/m), pH (0x0409, 100 x pH up to 0x0578) and Wind Speed (0x040b, 100 x m/s) as uint16 servers with the §4.1.3 range attributes and default reporting; the Concentration Measurement clusters 0x040c–0x0429 as single-precision fraction-of-one servers with NaN as the unknown value and a floating-point reportable change. Tolerance is defined but not instantiated by default. |
| PW-ZCL-LIGHT-001 | §5.2 | optional | implemented | `panweave-zcl::clusters::color_control` | Color Control cluster: hue/saturation, XY, color temperature, enhanced hue, color loop, commands and transitions | `hue_directions_and_timed_transition`, `xy_temperature_and_color_loop`, `color_control_through_the_dispatcher_and_scenes`, `level_couples_the_colour_temperature_and_pwm_runs_beside_it`, `primaries_color_points_and_drift_compensation` | Colour information attributes per ColorCapabilities, the Options / OptionsMask / OptionsOverride and ExecuteIfOff rules, Move to / Move / Step for hue (8-bit and enhanced, with the shortest / longest / up / down directions on the hue circle), saturation, XY and colour temperature (physical and per-command bounds, clipping), Move to Hue and Saturation, Color Loop Set with the update flags and stored hue, Stop Move Step, RemainingTime, the scene extension field set, StartUpColorTemperatureMireds and the Level Control coupling of §5.2.2.1.1 (CoupleColorTempToLevel), executed by the dispatcher on a 100 ms tick; the Defined Primaries / Additional Defined Primaries sets (up to six primaries), the Defined Colour Points set with the white point and DriftCompensation / CompensationText as optional extensions of a server (the runtime's 36-attribute instances hold a full-capability lamp without the primaries sets). The colour-mode conversions of §5.2.2.3.2 are manufacturer dependent as the specification states: a command in another colour mode starts from that mode's current attributes. |
| PW-ZCL-LIGHT-002 | §5.3 | optional | implemented | `panweave-zcl::clusters::configuration` | Ballast Configuration cluster | `ballast_levels_factor_and_burn_hours` | Ballast information / settings and lamp information / settings sets with the MinLevel / MaxLevel / factor / string-length guards of §5.3.2.2, the dimming curve output_for_level (linear between MinLevel and MaxLevel scaled by BallastFactorAdjustment), BallastStatus and the LampBurnHours trip-point alarm (code 0x01) counted by add_burn_hours; facade Dimmable Ballast device. PowerOnLevel / PowerOnFadeTime (deprecated) are defined but not instantiated. |
| PW-ZCL-HVAC-001 | §6.3, §6.4 | optional | implemented | `panweave-zcl::clusters::hvac` | Thermostat and Fan Control clusters | `setpoints_limits_dead_band_and_running_mode`, `weekly_schedule_set_get_clear`, `weekly_schedule_runs_against_the_clock`, `setpoint_tracking_ac_information_and_relay_log`, `fan_modes_follow_the_sequence` | Thermostat information and settings attributes with the setpoint limit, dead-band and control-sequence rules enforced on writes, Setpoint Raise/Lower executed by the dispatcher (ZclEvent::Setpoints), the Table 6-17 running mode (EmergencyHeatDelta selecting emergency heat), the scene extension field set and heating-only / cooling-only / dual builders; the weekly schedule extension (StartOfWeek, NumberOfWeeklyTransitions, NumberOfDailyTransitions, TemperatureSetpointHold; Set / Get / Clear Weekly Schedule with INSUFFICIENT_SPACE, INVALID_VALUE, FAILURE and INVALID_FIELD refusals, Get Weekly Schedule Response) run against the endpoint Time server per ADR-0015; the setpoint change tracking set (source, amount, UTC timestamp, setbacks clamped into their bounds on write, EmergencyHeatDelta), the AC information set with TemperatureSetpointHoldDuration, ThermostatProgrammingOperationMode (reported) and ThermostatRunningState, and the relay status log (record_relay_status FIFO, Get Relay Status Log answered LIFO with the unread count, the newest again once read out); Fan Control with FanMode validated against FanModeSequence; facade Thermostat device. The relay bit mapping, humidity sampling and hold-duration expiry are the application's; the schedule runs only while ThermostatProgrammingOperationMode bit 0 (schedule programming) is set when that attribute is present. |
| PW-ZCL-HVAC-002 | §6.2, §6.5, §6.6 | optional | implemented | `panweave-zcl::clusters::configuration` | Pump Configuration and Control, Dehumidification Control and Thermostat User Interface Configuration clusters | `pump_modes_status_and_alarms`, `dehumidification_demand_follows_setpoint_and_hysteresis`, `thermostat_ui_and_shade_guards` | Pump Configuration and Control: the mandatory information, dynamic and settings attributes (optional limits definable), OperationMode / ControlMode guards, the Figure 6-3 effective-mode resolution with remote sensors, PumpStatus bits, Capacity / Speed / Running updates and the Table 6-9 alarm codes gated by AlarmMask; Dehumidification Control with the range guards and a setpoint-plus-hysteresis demand; Thermostat User Interface Configuration with the value guards. The pump's own regulation loop and the Level Control setpoint coupling are the application's. |
| PW-ZCL-CLOS-001 | §7.3, §7.4 | optional | implemented | `panweave-zcl::clusters::window_covering` | Door Lock and Window Covering clusters | `lock_operations_follow_mode_actuator_and_pin_rules`, `wrong_codes_lock_the_lock_out_and_raise_the_tamper_alarm`, `unlock_with_timeout_and_auto_relock_lock_again`, `pin_user_table_commands`, `rfid_codes_share_the_user_table`, `schedules_gate_the_users_and_holidays_switch_the_mode`, `the_log_records_events_when_enabled`, `pin_programming_unlock_with_timeout_and_auto_relock`, `commands_are_validated_against_the_control`, `scenes_become_timed_go_to_commands`, `go_to_lift_percentage_reaches_the_application_and_the_position_is_reported` | Door Lock (§7.3): information / operational / security / event-mask attributes (user and schedule counts, PIN and RFID lengths, EnableLogging, SecurityLevel, Language / LED / sound with guards), Lock, Unlock, Toggle and Unlock with Timeout with the operating-mode (Privacy and No RF refuse with FAILURE), ActuatorEnabled, RequirePINforRFOperation, WrongCodeEntryLimit lockout (tamper alarm 4 via the Alarms cluster) and AutoRelockTime rules; a bounded user table with PIN and RFID codes (Set / Get / Clear / Clear All for both, Set / Get User Status and Type; clearing one code keeps a user holding the other), week day and year day schedules per user (Set / Get / Clear with INVALID_FIELD / NOT_FOUND responses) that gate schedule-type users at RF operations against the endpoint clock (invalid-schedule events, refused without a clock), holiday schedules switching OperatingMode from the dispatcher tick and restoring it afterwards, Operation and Programming Event Notifications gated by the masks with codes masked unless SendPINOverTheAir, an eight-record FIFO event log served by Get Log Record when EnableLogging is set, the SecurityLevel = APS policy (commands without APS security answer NOT_AUTHORIZED) and the LockState scene extension as a delayed operation. Keypad / manual events come from the application (door_lock_operation_event). Window Covering (§7.4): information and settings attributes, open-/closed-loop lift and tilt, the seven commands with INVALID_VALUE / UNSUPPORTED_COMMAND validation and the Mode maintenance bit (refused with FAILURE, a status the spec does not name for this case), Mode reversal mirrored into ConfigStatus, percentage reporting by default, the scene extension recalled as a timed go-to command (ZclEvent::WindowCovering); Go To Value accepts any value between the two installed limits whichever is larger; intermediate setpoint strings and the velocity / acceleration attributes are not instantiated. |
| PW-ZCL-CLOS-002 | §7.2, §7.5 | optional | implemented | `panweave-zcl::clusters::configuration` | Shade Configuration and Barrier Control clusters | `thermostat_ui_and_shade_guards`, `barrier_commands_events_and_safety` | Shade Configuration (information and settings sets, Status with only the motor-direction bit writable, Mode and ClosedLimit guards, set_motion); Barrier Control with Go To Percent / Stop executed by the dispatcher (ZclEvent::Barrier, INVALID_VALUE for partial positions without the capability, FAILURE under Remote Lockout), the open / close and command event counters that stick at 0xfffe, SafetyStatus changes reported as alarm codes, reporting of MovingState / SafetyStatus / BarrierPosition and the BarrierPosition scene field. The OpenPeriod / ClosePeriod watchdog is the application's. |
| PW-ZCL-IAS-001 | §8.2 | optional | implemented | `panweave-zcl::clusters::ias_zone` | IAS Zone cluster: enrolment (auto/trip-to-pair), zone status change notification, enroll request/response | `auto_enroll_request_flow_and_notifications`, `trip_to_pair_requests_on_status_change`, `ias_zone_auto_enroll_request_and_status_notification` | Zone information / settings attributes, the three enrolment procedures (Trip-to-Pair via Zcl::request_zone_enrollment or a status change while unenrolled, Auto-Enroll-Response, Auto-Enroll-Request on the IAS_CIE_Address write), Zone Status Change Notification with the Delay field, Zone Enroll Request / Response, Initiate Normal Operation / Test Mode with the Test bit and sensitivity level, all executed by the dispatcher (ZclEvent::ZoneEnrolled / ZoneTestMode). Sender authentication uses the network address of the node that wrote IAS_CIE_Address (the dispatcher has no IEEE resolution); the NOT_AUTHORIZED rejection of ZDO bind requests from other nodes (§8.2.2.1.3) is not implemented. |
| PW-ZCL-IAS-002 | §8.3, §8.4 | optional | implemented | `panweave-zcl::clusters::ias_wd` | IAS ACE and IAS WD clusters | `zone_queries_answer_from_the_table`, `arm_validates_the_code_and_tracks_the_panel`, `bypass_results_and_the_list_cleared_on_disarm`, `emergencies_and_status_changes`, `warning_round_trips_and_is_clamped`, `squawk_round_trips`, `keypad_arms_the_cie_which_warns_the_siren_and_relays_zone_status` | IAS ACE (§8.3): a bounded zone table (16 entries; the spec allows 255) with panel status, bypass list and arm/disarm code (redacted Debug, constant-time compare); Get Zone ID Map, Get Zone Information, Get Panel Status, Get Bypassed Zone List (Set Bypassed Zone List) and Get Zone Status (mask flag, paging) answered by the dispatcher; Arm with Invalid Code / Already Disarmed / Not Ready notifications and the panel status transition, disarming clearing the bypass list (§8.3.2.4.7.4); Bypass with the Table 8-18 results; Emergency / Fire / Panic to the application (ZclEvent::Ace); Zone Status Changed and Panel Status Changed to the bound clients from the application helpers, and a CIE endpoint's IAS Zone client relaying Zone Status Change Notifications into the panel. The per-second Panel Status Changed during arming delays is the application's (it calls ace_set_panel_status). IAS WD (§8.4): MaxDuration, Start Warning (duration clamped, duty cycle rounded to tens, a new command ending the previous one, expiry timer → ZclEvent::Warning None), Squawk ignored while warning. |
| PW-ZCL-OTA-001 | §11 | optional | implemented | `panweave-zcl::clusters::ota` | OTA Upgrade cluster: image notify, query next image, image block/page, upgrade end, file format, signature verification | `header_and_sub_elements_round_trip`, `download_flow_between_client_and_server`, `page_request_is_served_block_by_block`, `wait_forever_is_requeried_hourly_then_applied`, `device_specific_file_request_round_trips`, `client_downloads_verifies_and_is_told_when_to_upgrade`, `client_discovers_the_first_server_and_stores_its_ieee_address`, `preprogrammed_server_is_located_by_address`, `an_idle_client_polls_the_server_periodically` | OTA file header (mandatory and optional fields, suitability checks) and sub-element iteration, the client attributes, codecs for every §11.13 command (Image Notify, Query Next Image, Image Block / Page Request, Image Block Response, Upgrade End, Query Device Specific File), the Table 11-15 time interpretation, a client download machine (notify jitter and matching rules, query, block requests with rate limiting and waits, out-of-order block re-requests, verify hand-off, Upgrade End, activation policy, the §11.16 hourly re-query while waiting indefinitely with self-applied upgrade after three unanswered queries), server helpers answering from an ImageSource including PageService for Image Page Request (§11.13.7.4), and upgrade server discovery per §11.8 (Stack::discover_ota_server: NWK_addr_req for a preprogrammed UpgradeServerID, else Match_Desc_req then IEEE_addr_req, application link key requested for a non-Trust-Center server); simulator tests download a 300-octet image and discover servers. The idle client polls the server every application-set period (Client::with_query_period, §11.8.2 / §11.13.4), re-armed after each answer. The ECDSA signature verification of §11.6–§11.7 is the host's (the client hands the downloaded image to the application for verification before Upgrade End, ADR-0012: no hand-rolled ECC), and the periodic rediscovery of the upgrade server (§11.8) is the application's call on Stack::discover_ota_server. |
| PW-ZCL-COMM-001 | §13.2 | optional | implemented | `panweave-zcl::clusters::commissioning` | Commissioning cluster (startup attribute set) | `attributes_round_trip_and_keys_are_write_only`, `consistency_per_startup_control`, `restart_checks_consistency_and_reports_the_set`, `save_restore_reset`, `saved_sets_round_trip_through_storage`, `writes_are_guarded`, `tool_writes_the_startup_set_and_restarts_the_router_into_a_silent_join` | The startup parameters, join, end-device and concentrator attribute sets (TrustCenterMasterKey omitted); NetworkKey and PreconfiguredLinkKey are write-only (reads answer NOT_AUTHORIZED per §13.2.2.2.1) and redacted; write guard for StartupControl / ShortAddress / PANId / StackProfile and the rejoin-without-EPID FAILURE (§13.2.2); Restart Device with the Table 13-5 consistency check, the response before the restart, StackEvent::Restart and the stack restarting itself after delay + RAND(jitter x 80 ms) (install the set, or resume the running configuration); Save / Restore / Reset Startup Parameters over a two-entry store kept in non-volatile storage (Kind::StartupSets, restored on warm start) with the §13.2.2.3.4 option semantics. Stack::restart_from_startup_set forms / silently adopts / rejoins / associates after the leave completes and wires ScanAttempts, TimeBetweenScans, RejoinInterval, MaxRejoinInterval, ParentRetryThreshold, IndirectPollRate and the concentrator parameters into the stack configuration; the seeded set comes from the live NIB. |
| PW-ZCL-COMM-002 | §13.3 | optional | implemented | `panweave-zcl::clusters::touchlink` | Touchlink commissioning cluster (inter-PAN commands, utility commands) | `scan_request_and_response_round_trip`, `start_join_update_round_trip`, `information_and_utility_round_trip`, `update_id_comparison_handles_wrap`, `certification_key_test_vectors`, `development_key_test_vector` | Codecs of every inter-PAN command (Scan Request / Response with the single sub-device fields, Device Information Request / Response, Identify, Reset To Factory New, Network Start / Join Router / Join End Device Requests and Responses, Network Update Request) and the utility commands (Endpoint Information, Get Group Identifiers, Get Endpoint List), the ZigBee and Touchlink information fields, the constants of Table 13-21, the nwkUpdateId comparison of §13.3.4.10 and the key transport algorithms of §13.3.4.11 in panweave-security::touchlink (development, master, certification; the published test vectors pass). The utility commands are codecs only: the dispatcher does not answer Get Group Identifiers / Get Endpoint List (TODO(PW-ZCL-TOUCHLINK-UTILITY)). |
| PW-ZCL-MTRID-001 | §10.13 | optional | implemented | `panweave-zcl::clusters::meter_identification` | Meter Identification cluster | `server_carries_the_mandatory_set` | The twelve attributes of Table 10-215 (CompanyName, MeterTypeID, DataQualityID, CustomerName (writable), Model, PartNumber, ProductRevision, SoftwareRevision, UtilityName, POD, AvailablePower, PowerThreshold) with the Meter Type and Data Quality enumerations; the server builder takes the mandatory set. The cluster has no commands. |
| PW-ZCL-APPL-001 | §15 | optional | implemented | `panweave-zcl::clusters::appliance` | Appliance Management clusters: EN50523 Appliance Control, Appliance Identification, Appliance Events and Alerts, Appliance Statistics | `control_codecs_round_trip`, `control_server_handles_commands`, `identification_packs_the_basic_fields`, `alerts_are_recorded_and_answered`, `statistics_queue_serves_log_requests`, `appliance_control_is_executed_by_the_application`, `alerts_events_and_logs_are_notified_and_served` | Appliance Control (§15.2): StartTime / FinishTime / RemainingTime with the Table 15-4 time encoding, Execution of a Command (ZclEvent::ApplianceCommand), Signal State answered from the stored signal state (status, remote enable flags, device status 2 structure, optional 24-bit status 2), Write Functions records (codec; the command is handed to the application, which owns the functions), Overload Pause / Resume / Warning (ZclEvent::ApplianceOverload), Signal State Notification to the bound clients from Zcl::appliance_signal_state. Appliance Identification (§15.3): BasicIdentification packing (company, brand, product type, CECED version) and the extended attribute definitions. Events and Alerts (§15.4): a 15-entry alert list (present replaces, recovery removes), Get Alerts answered by the dispatcher, Alerts Notification and Event Notification from Zcl::appliance_alert / appliance_event. Statistics (§15.5): LogMaxSize (70) / LogQueueMaxSize (8), an 8-entry log queue with consecutive Log IDs, Log Notification on Zcl::appliance_log (time-stamped from a Time server on the endpoint, else 0xffffffff), Log Request (NOT_FOUND for an unknown id), Log Queue Request and Statistics Available. Bulk logs over the Partition cluster are not supported (the cluster is not implemented, PW-ZCL-MISC-001). |
| PW-ZCL-MISC-001 | §9, §12, §14 | optional | not-implemented | `panweave-zcl::clusters` | Protocol interface (Generic / BACnet / ISO 7818 tunnels, Partition), telecommunications and retail clusters | — | Low priority; metadata entries only. The Partition cluster (§9.6) would extend Appliance Statistics to bulk logs (PW-ZCL-APPL-001). |

## R23.2 — Zigbee Specification, Revision 23.2

| Id | Section | Level | Status | Module | Summary | Tests | Notes |
|---|---|---|---|---|---|---|---|
| PW-R23-GEN-001 | §1.2.5 | mandatory | implemented | `panweave-nwk::layer::rx` | Malformed Zigbee/802.15.4 frames are discarded without response and without state change | `frame_payload_is_borrowed`, `software_filter_drops_foreign_frames`, `malformed_headers_are_rejected`, `aps_layer_survives_arbitrary_frames`, `dispatcher_is_total` | Malformed frames are dropped and counted at every layer (MAC frame decode, NWK stats.malformed, APS stats.malformed, ZDO and ZCL decode failures) without a response or state change; property tests feed arbitrary bytes through the APS layer and the ZCL dispatcher. |
| PW-R23-GEN-002 | §1.2.6, Annex I.1–I.2 | mandatory | implemented | `panweave-codec::tlv` | TLV encoding: tag, length-minus-one, value; unknown tags skipped; malformed TLVs invalidate the message; global tag range 64–255 | `iterates_well_formed_tlvs`, `truncated_header_or_value_is_rejected`, `duplicate_tags_rejected_except_manufacturer_specific`, `nested_encapsulation_rejected` |  |
| PW-R23-GEN-003 | Annex I.4 | mandatory | implemented | `panweave-nwk::tlv` | Global TLVs 64–76 (manufacturer specific, supported key negotiation methods, PAN ID conflict report, next PAN ID/channel change, symmetric passphrase, router information, fragmentation parameters, joiner/beacon appendix encapsulation, configuration parameters, device capability extension) | `key_negotiation_methods_round_trip_and_min_length`, `fixed_size_tlvs`, `joiner_encapsulation_nesting` | All 13 global TLVs decode and encode; the BDB Encapsulation Global TLV (74) is carried opaquely, which is complete: BDB 3.1 section 14 defines no local TLVs inside it. |
| PW-R23-NWK-001 | §3.3.1 | mandatory | implemented | `panweave-nwk::frame` | General NPDU header layout: 16-bit frame control, destination and source short addresses, radius, sequence number, optional destination/source IEEE addresses, optional source-route subframe | `data_header_round_trip`, `command_header_with_ieee_addresses` |  |
| PW-R23-NWK-002 | §3.3.1.1 | mandatory | implemented | `panweave-nwk::frame::FrameControl` | Frame control sub-fields: frame type (data, command, inter-PAN; one reserved), protocol version, discover route, deprecated multicast flag, security, source route, dst/src IEEE presence, end-device initiator; reserved bits | `data_header_round_trip`, `presence_flag_mismatch_is_rejected_on_encode` |  |
| PW-R23-NWK-003 | §3.3.1.8 | conditional | implemented | `panweave-nwk::frame::SourceRoute` | Source route subframe: relay count, relay index (initialised to count-1 and decremented per hop), relay list ordered nearest-destination first | `source_route_subframe` |  |
| PW-R23-NWK-004 | §3.3.2.2, §3.4 | mandatory | implemented | `panweave-nwk::command` | NWK command frame: command identifier octet followed by command payload; command frames suppress route discovery and use originator source address | `route_request_and_reply`, `status_leave_rejoin_record` |  |
| PW-R23-NWK-005 | §3.4.1 | mandatory | implemented | `panweave-nwk::command::RouteRequest` | Route Request command: command options (many-to-one, dst IEEE present, multicast deprecated), route request identifier, destination address, path cost, optional destination IEEE address, optional TLVs | `route_request_and_reply` |  |
| PW-R23-NWK-006 | §3.4.2 | mandatory | implemented | `panweave-nwk::command::RouteReply` | Route Reply command: options (originator/responder IEEE presence, multicast deprecated), identifier, originator and responder addresses, path cost, optional IEEE addresses | `route_request_and_reply` |  |
| PW-R23-NWK-007 | §3.4.3 | mandatory | implemented | `panweave-nwk::command::NetworkStatus` | Network Status command: status code and destination address; defined status codes incl. no route available, tree link failure, non-tree link failure, low battery, no routing capacity, no indirect capacity, indirect transaction expiry, target device unavailable, target address unallocated, parent link failure, validate route, source route failure, many-to-one route failure, address conflict, verify addresses, PAN identifier update, network address update, bad frame counter, bad key sequence number, unknown command | `status_leave_rejoin_record` |  |
| PW-R23-NWK-008 | §3.4.4 | mandatory | implemented | `panweave-nwk::command::Leave` | Leave command: options rejoin, request, remove children | `status_leave_rejoin_record` |  |
| PW-R23-NWK-009 | §3.4.5 | mandatory | implemented | `panweave-nwk::command::RouteRecord` | Route Record command: relay count and relay list accumulated toward the concentrator | `status_leave_rejoin_record` |  |
| PW-R23-NWK-010 | §3.4.6, §3.4.7 | mandatory | implemented | `panweave-nwk::command::{RejoinRequest, RejoinResponse}` | Rejoin Request (capability information) and Rejoin Response (short address, status) commands | `status_leave_rejoin_record` |  |
| PW-R23-NWK-011 | §3.4.8 | mandatory | implemented | `panweave-nwk::command::LinkStatus` | Link Status command: options (entry count, first frame, last frame) and list of neighbor address with incoming/outgoing cost | `link_status_entries` |  |
| PW-R23-NWK-012 | §3.4.9, §3.4.10 | mandatory | implemented | `panweave-nwk::command::{NetworkReport, NetworkUpdate}` | Network Report (PAN ID conflict report with list of PAN IDs) and Network Update (PAN ID update with update id and new PAN ID) commands | `report_update_and_timeouts` |  |
| PW-R23-NWK-013 | §3.4.11, §3.4.12 | mandatory | implemented | `panweave-nwk::command::{EndDeviceTimeoutRequest, EndDeviceTimeoutResponse}` | End Device Timeout Request (requested timeout enumeration, configuration) and Response (status, parent information bitmask) | `report_update_and_timeouts` |  |
| PW-R23-NWK-014 | §3.4.13 | optional | implemented | `panweave-nwk::layer::power` | Link Power Delta command: type (notification, request, response), list of device address and power delta | `neighbors_negotiate_transmit_power_through_link_power_delta` | Codec and the section 3.6.11 behaviour (see PW-R23-NWK-064). |
| PW-R23-NWK-015 | §3.4.14, §3.4.15 | mandatory | implemented | `panweave-nwk::command::{CommissioningRequest, CommissioningResponse}` | Network Commissioning Request (association type, capability information, TLVs) and Response (status, short address, TLVs) | `power_delta_and_commissioning` |  |
| PW-R23-NWK-016 | §3.5.1, §3.5.2 | mandatory | implemented | `panweave-nwk::nib` | NWK constants (protocol version, max depth, discovery retry backoff, route discovery time, RREQ retries/retry interval, min RREQ jitter, initial RREQ retries) and NIB attributes with defaults and ranges | `defaults_and_sequence`, `clear_for_leave_resets_identity` |  |
| PW-R23-NWK-020 | §3.6.1.5 | mandatory | implemented | `panweave-nwk::layer::join` | Network discovery: active scan over requested channels, collect beacons with Zigbee payload into a network descriptor list; report to NLME-NETWORK-DISCOVERY.confirm | `coordinator_forms_and_end_device_joins_and_exchanges_data`, `ranking_prefers_hub_then_update_id_then_lqa`, `beacon_payload_round_trip_and_bit_layout` |  |
| PW-R23-NWK-021 | §3.6.1.2 | mandatory | implemented | `panweave-nwk::layer::join` | Network formation by coordinator (or distributed router): energy scan + active scan, select channel with least energy/fewest networks, select PAN ID not in use, choose extended PAN ID, set short address 0x0000 (or random for distributed), start MAC | `coordinator_forms_and_end_device_joins_and_exchanges_data` |  |
| PW-R23-NWK-022 | §3.6.1.3 | mandatory | implemented | `panweave-nwk::layer::join` | Permit joining: router/coordinator enables MAC association for the given duration (0x00 disables, 0xFF deprecated → bounded), permit duration timer, beacon association-permit bit reflects state | `coordinator_forms_and_end_device_joins_and_exchanges_data`, `association_denied_when_permit_off` |  |
| PW-R23-NWK-023 | §3.6.1.6.1.1 | mandatory | implemented | `panweave-nwk::layer::join` | Joining via MAC association: parent selection from neighbor table (permit joining, depth, link cost, end-device capacity), MAC associate, receive short address, update NIB and neighbor table, report to NLME-JOIN.confirm | `coordinator_forms_and_end_device_joins_and_exchanges_data`, `router_joins_and_routes_between_end_devices_of_different_parents` |  |
| PW-R23-NWK-024 | §3.6.1.6.1.3 | mandatory | implemented | `panweave-nwk::layer::join` | Parent processing of association: accept only when permit joining, capacity available, assign stochastic short address, add child to neighbor table with relationship child and keep-alive state, respond with association response | `coordinator_forms_and_end_device_joins_and_exchanges_data`, `association_indication_and_indirect_response_parent_side` |  |
| PW-R23-NWK-025 | §3.6.1.6.1.2 | mandatory | implemented | `panweave-nwk::layer::join` | Rejoin: NWK-layer Rejoin Request to a selected parent (secured with NWK key when available, unsecured otherwise), Rejoin Response with address/status, retries and timeout | `secured_rejoin_after_reboot_keeps_address` |  |
| PW-R23-NWK-026 | §3.6.1.6.1.3 | mandatory | implemented | `panweave-nwk::layer::join` | Parent processing of Rejoin Request: verify capacity/permission, allocate or keep address, respond with Rejoin Response, notify higher layer (NLME-JOIN.indication with rejoin flag and secure flag) | `secured_rejoin_after_reboot_keeps_address` |  |
| PW-R23-NWK-027 | §3.6.1.6, §3.4.14, §3.4.15 | mandatory | implemented | `panweave-nwk::layer::join` | Joining via Network Commissioning Request (R23): NWK-layer association carrying capability information and TLVs, response with status and allocated address | `power_delta_and_commissioning`, `replacement_trust_center_takes_over_from_a_hashed_backup`, `joins_and_rejoins_through_enhanced_beacons`, `joining_list_gates_association_and_is_read_and_pushed` | Every simulator join between R23 devices uses the Network Commissioning Request (initial join with key negotiation TLVs, rejoin, Trust Center rejoin); the parent side allocates addresses, answers with the status and address and gates on the joining policy. |
| PW-R23-NWK-028 | §3.6.1.7 | mandatory | implemented | `panweave-nwk::address_map` | Stochastic network address assignment: random 16-bit address in the unicast range, avoiding 0x0000, reserved and broadcast ranges and known addresses | `stochastic_allocation_avoids_used_and_reserved` |  |
| PW-R23-NWK-029 | §3.6.1.10 | mandatory | implemented | `panweave-nwk::layer::join` | Address conflict detection and resolution: detect conflicting short/IEEE pairs from any frame, send Network Status (address conflict), re-assign own address when local conflict, handle child conflicts via Unicast Rejoin Response | `duplicate_addresses_are_detected_and_reassigned` | A router seen behind another device short address triggers the Network Status (address conflict) broadcast; the devices holding the address pick fresh ones (stochastic), announce them (Device_annce, StackEvent::AddressChanged) and end device children are re-addressed with an unsolicited Rejoin Response. |
| PW-R23-NWK-030 | §3.6.1.11 | mandatory | implemented | `panweave-nwk::layer::join` | Leave: locally initiated (NLME-LEAVE.request) sends Leave command with rejoin/remove-children options and resets NIB; remotely received Leave with request flag from parent (and per nwkLeaveRequestAllowed policy) causes leave; children removed when remove-children is set | `leave_and_child_aging` |  |
| PW-R23-NWK-031 | §3.6.1.12 | mandatory | implemented | `panweave-runtime::persist` | Reset of NWK layer: warm reset preserves persisted NIB attributes; cold reset clears tables | `secured_rejoin_after_reboot_keeps_address`, `end_device_and_coordinator_survive_reboots` | Stack::restore performs the warm start (NIB, keys, children restored; frame counters continue after the last persisted reservation); a cold reset is a fresh stack over erased storage (Stack::erase_persisted). |
| PW-R23-NWK-032 | §3.6.1.7 | mandatory | implemented | `panweave-nwk::neighbor` | Neighbor table contents: extended/short address, device type, rx-on-when-idle, relationship, transmit failure, LQI, outgoing cost, age, incoming beacon timestamp, keep-alive/timeout fields, MAC interface | `insert_evicts_only_stale_routers`, `child_aging_and_security_timer`, `path_cost_uses_max_of_directions` |  |
| PW-R23-NWK-033 | §3.6.1.13 | mandatory | implemented | `panweave-nwk::layer::join` | PAN ID conflict: detection from beacons with same PAN ID and different extended PAN ID, Network Report to network manager, network manager selects new PAN ID and broadcasts Network Update, all devices switch after nwkNetworkBroadcastDeliveryTime | `report_update_and_timeouts`, `pan_id_conflicts_are_counted_and_changes_are_explicit`, `network_manager_changes_the_pan_id_explicitly` | R23 semantics (ADR-0010): detection only counts nwkPanIdConflictCount, legacy Network Reports become NLME-NETWORK-STATUS 0x14, the PAN ID changes only through Nwk::change_pan_id (staged nwkNextPanId or random), receivers gate the Network Update by their staged nwkNextPanId and switch after nwkNetworkBroadcastDeliveryTime. |
| PW-R23-NWK-040 | §3.6.2.1 | mandatory | implemented | `panweave-nwk::layer::tx` | Data transmission: construct NPDU with radius (default 2×nwkMaxDepth), sequence number, addressing, security per nwkSecurityLevel, routing decision (neighbor, routing table, route discovery, many-to-one, source route) and MAC data request with ack for unicast | `sleepy_end_device_receives_via_indirect_delivery` | Frames for sleepy children (unicasts and this device's own broadcast copies) are registered with the MAC as deferred indirect transactions and secured only when the child polls, so the frame counter of a held frame is never overtaken by frames transmitted before the poll (freshness at the receiver, §3.6.2.2); relayed broadcasts keep the originator's protection. |
| PW-R23-NWK-041 | §3.6.2.2 | mandatory | implemented | `panweave-nwk::layer::rx` | Reception and relay: validate header, decrement radius, discard when radius zero, verify end-device initiator and source consistency, deliver to NLDE when destined locally, relay otherwise; unknown frame types dropped | `router_joins_and_routes_between_end_devices_of_different_parents` |  |
| PW-R23-NWK-042 | §3.6.2.3 | mandatory | implemented | `panweave-nwk::nib` | Sequence number handling: incremented per new transmitted frame, seeded randomly | `defaults_and_sequence` |  |
| PW-R23-NWK-043 | §3.6.3 | mandatory | implemented | `panweave-nwk::neighbor::cost` | Link cost estimation from LQI: 1–7 with 7 for unusable; incoming cost from local measurement, outgoing cost from Link Status | `link_cost_table_3_76`, `lqa_median_filter` |  |
| PW-R23-NWK-050 | §3.6.4.2 | mandatory | implemented | `panweave-nwk::routing` | Routing table and route discovery table entries with status enumeration (active, discovery underway, discovery failed, inactive, validation underway), many-to-one flags, route record required flag, expiry | `routing_table_insert_and_eviction`, `discovery_table_expiry_and_reuse` |  |
| PW-R23-NWK-051 | §3.6.4.5.1 | mandatory | implemented | `panweave-nwk::layer::route` | Route discovery initiation: create RDT entry, broadcast Route Request with radius, retry nwkcInitialRREQRetries then nwkcRREQRetries with jitter, expire after nwkcRouteDiscoveryTime | `router_joins_and_routes_between_end_devices_of_different_parents` |  |
| PW-R23-NWK-052 | §3.6.4.5.1.2–§3.6.4.5.1.4 | mandatory | implemented | `panweave-nwk::layer::route` | Route Request processing at intermediate/destination: path cost accumulation, RDT update on better cost, rebroadcast with jitter, destination (or neighbor of destination) generates Route Reply | `router_joins_and_routes_between_end_devices_of_different_parents`, `suitability_rules` |  |
| PW-R23-NWK-053 | §3.6.4.5.2 | mandatory | implemented | `panweave-nwk::layer::route` | Route Reply processing: forward along RDT sender toward originator with cost accumulation, update routing table to active at originator | `router_joins_and_routes_between_end_devices_of_different_parents` |  |
| PW-R23-NWK-054 | §3.6.4.5.1 | mandatory | implemented | `panweave-nwk::layer::route` | Many-to-one route discovery from concentrator: Route Request with many-to-one flag, no Route Reply, routers install route to concentrator and mark route record required when concentrator has no memory | `concentrator_learns_routes_from_route_records_and_source_routes_back` | Nwk::route_discovery(None, ..) advertises the concentrator (many-to-one Route Request, no Route Reply); routers install the route to it and, without a route cache, owe a Route Record. |
| PW-R23-NWK-055 | §3.6.4.5.5 | mandatory | implemented | `panweave-nwk::layer::route` | Route Record: originator sends Route Record before data when route record required; relays append their address; concentrator stores source route | `concentrator_learns_routes_from_route_records_and_source_routes_back` | Route Record precedes data from an originator (own frames and on behalf of end device children) whose route to the concentrator has route record required; relays append their address; the concentrator stores the relay list in its source route table. |
| PW-R23-NWK-056 | §3.6.4.3.1, §3.6.4.3.2 | mandatory | implemented | `panweave-nwk::layer::tx` | Source routing: originator inserts relay list; relays forward to next relay by index; source route failure reported with Network Status | `concentrator_learns_routes_from_route_records_and_source_routes_back` | A concentrator with a stored route record originates source routed frames (relay index count-1, first hop the relay nearest to it, section 3.6.4.3.1); relays forward by index and the destination delivers; a source route failure is reported with a Network Status. |
| PW-R23-NWK-057 | §3.6.4.8 | mandatory | implemented | `panweave-nwk::layer::tx` | Route maintenance: count transmission failures per neighbor, initiate route repair / Network Status (no route available) toward originator on failure, route expiry after nwkRouteExpiryTime | `routing_table_insert_and_eviction`, `router_joins_and_routes_between_end_devices_of_different_parents` |  |
| PW-R23-NWK-058 | §3.6.6 | mandatory | implemented | `panweave-nwk::layer::bcast` | Broadcast: broadcast transaction table with sequence/source, passive acknowledgement from neighbors, retransmission up to nwkMaxBroadcastRetries with nwkPassiveAckTimeout, jitter, radius handling, broadcast address semantics (0xFFFF all, 0xFFFD rx-on, 0xFFFC routers, 0xFFFB low-power routers) | `coordinator_forms_and_end_device_joins_and_exchanges_data`, `duplicate_detection_and_expiry_reuse`, `passive_ack_tracking` |  |
| PW-R23-NWK-059 | §3.6.7 | deprecated | implemented | `panweave-nwk::frame` | NWK multicast (deprecated in R23): frames with multicast flag are treated per deprecation rules | `data_header_round_trip` | Multicast flag is parsed and ignored; such frames are handled as broadcasts. |
| PW-R23-NWK-060 | §3.6.8 | mandatory | implemented | `panweave-nwk::beacon` | Beacon payload: protocol id, stack profile, protocol version, router/end-device capacity, device depth, extended PAN ID, tx offset, update id; optional beacon appendix TLVs | `beacon_payload_round_trip_and_bit_layout`, `trust_center_sets_the_network_wide_beacon_appendix` | Beacon appendix built per §3.6.8.2: network-wide TLVs first, then the device's Router Information unless already set network-wide; TLVs that do not fit are omitted whole. |
| PW-R23-NWK-061 | §3.6.9 | mandatory | implemented | `panweave-runtime::persist` | Persistent NIB data set survives reset | `end_device_and_coordinator_survive_reboots` | PAN ID, extended PAN ID, network address, nwkUpdateId, parent address/IEEE, depth, parent information, stack profile, network manager and the end-device children (IEEE, short, End Device Configuration, Device Timeout) are stored on every NWK persist trigger; the MAC interface table is not modelled (single interface). |
| PW-R23-NWK-062 | §3.6.10 | mandatory | implemented | `panweave-nwk::layer::maint` | End-device aging: child timeout negotiated with End Device Timeout Request/Response; parent ages children on keep-alive (data poll / timeout request) and removes children on expiry; end device sends keep-alive at nwkEndDeviceTimeout / 3 or per parent info | `leave_and_child_aging`, `coordinator_forms_and_end_device_joins_and_exchanges_data`, `report_update_and_timeouts` |  |
| PW-R23-NWK-063 | §3.6.4.4 | mandatory | implemented | `panweave-nwk::layer::route` | Link Status periodic transmission every nwkLinkStatusPeriod by routers with all router neighbors; outgoing cost updated from received link status; neighbor age incremented and entries with age > nwkRouterAgeLimit dropped | `router_joins_and_routes_between_end_devices_of_different_parents`, `insert_evicts_only_stale_routers` |  |
| PW-R23-NWK-064 | §3.6.11 | optional | implemented | `panweave-nwk::layer::power` | Power negotiation via Link Power Delta | `neighbors_negotiate_transmit_power_through_link_power_delta` | Behind NwkConfig::power_control: routers and the coordinator broadcast periodic Link Power Delta notifications (nwkLinkPowerDeltaTransmitRate plus a one-off jitter, radius 1, all rx-on neighbours with a known RSSI), an rx-on end device unicasts a notification to its parent, a sleepy end device sends a request, polls for the response and applies only that; received commands are checked against the neighbour table and applied to the MAC Power Control Information Table (NwkAction::MacAdjustTxPower); a router answers end device children; the parent information bit 2 and §3.6.11.2 rate selection are honoured; rejoins and channel changes return to full power. Popt is configurable (NwkConfig::optimal_rssi_dbm, −65 dBm for 2.4 GHz). |
| PW-R23-NWK-065 | §3.6.12 | optional | partially-implemented | `panweave-nwk::interface` | Multiple MAC interfaces table | `set_and_get_interface_follow_3_2_2_34`, `interface_table_confines_channels_and_gates_routers` | The nwkMacInterfaceTable (Table 3-69) with NLME-SET-INTERFACE / NLME-GET-INTERFACE semantics (index lookup, last-interface rule, single supported channel, RoutersAllowed, duty cycle thresholds, link cost scalar, counters reset on read) is kept by the NWK layer: discovery, energy scans and formation use only channels an enabled interface supports, the entry records the channel in use, counts unicast traffic and its RoutersAllowed gates router joins; an EnhancedActive ScanType selects enhanced beacon requests. One radio is driven: the Multi-MAC selection and switch procedures of section 3.6.12.1/3.6.12.2 (joining per interface, bridging between interfaces, per-interface link status and broadcasts) are not implemented, and disabling an interface does not switch the radio off (the runtime owns the radio). |
| PW-R23-NWK-066 | §3.6.13 | mandatory | implemented | `panweave-nwk::layer::rx` | Unknown NWK command identifiers are silently discarded (router responds with Network Status unknown command where specified) | `power_delta_and_commissioning` |  |
| PW-R23-NWK-067 | §3.7 | mandatory | implemented | `panweave-types::status::NwkStatus` | NWK status values | `nwk_status_round_trip_and_unknown_preservation` |  |
| PW-R23-APS-001 | §2.2.5.1 | mandatory | implemented | `panweave-aps::frame` | APS frame header: frame control (frame type, delivery mode, ack format, security, ack request, extended header), destination endpoint, group address, cluster id, profile id, source endpoint, APS counter, extended header (fragmentation, block number, ack bitfield) | `data_header_round_trip`, `group_header_round_trip`, `command_and_ack_headers`, `fragmented_headers`, `malformed_headers_are_rejected` |  |
| PW-R23-APS-002 | §2.2.5.2 | mandatory | implemented | `panweave-aps::frame` | Data, command and acknowledgement frame layouts | `data_header_round_trip`, `command_and_ack_headers` |  |
| PW-R23-APS-003 | §2.2.7 | mandatory | implemented | `panweave-aps::aib` | APS constants and AIB attributes (ack wait duration, max frame retries, fragmentation parameters, binding/group tables, interframe delay, max window size) | `fragmented_asdu_is_reassembled`, `lost_acknowledgements_trigger_retries_then_no_ack` | Constants of Table 2-23 and scalar attributes of Tables 2-24/4-35; the statistics table (Table 2-26) and the fragmentation cache (Table 2-27) are not implemented (window size 1 needs no per-peer parameters). |
| PW-R23-APS-004 | §2.2.8.4.2 | mandatory | implemented | `panweave-aps::layer::tx` | Reliable unicast: APS ack request, retransmission up to apscMaxFrameRetries with apscAckWaitDuration, confirm with status | `acked_unicast_is_delivered_once_and_confirmed`, `lost_acknowledgements_trigger_retries_then_no_ack` | Retransmissions of APS-secured frames use a fresh frame counter; NWK failures of acknowledged frames are retried like a lost acknowledgement. |
| PW-R23-APS-005 | §2.2.8.4.5 | conditional | implemented | `panweave-aps::layer` | Fragmentation and reassembly: block numbers, window size, ack bitfield, interframe delay, reassembly timeout, per-transaction state | `fragmented_asdu_is_reassembled`, `fragment_loss_is_recovered_by_retransmission`, `fragmented_messages_wait_for_the_peer_parameters` | Fragmented transmission and reassembly with the Table 2-28 parameters every R23 node advertises (apscWindowSize 1, apscMaxASDU at least 128): block numbers, per-block acknowledgements with the extended header and ack bitfield, retries and the receive-side persistence; a legacy peer transmitting with a larger window converges through the lowest-unacknowledged-block retransmission of §2.2.8.4.5.3. Fragmentation discovery and caching per §2.2.8.4.5.1: an ASDU needing fragmentation to a unicast peer not in apsFragmentationCacheTable is held while the peer's Node Descriptor is fetched (MaximumIncomingTransferSize and the Fragmentation Parameters TLV), the result cached (16 entries, dropped when the device leaves) and the message sent or refused (StackEvent::FragmentationRefused) accordingly; the application may seed the cache. Transmit windows larger than 1 are not offered (the specification mandates 1). |
| PW-R23-APS-006 | §2.2.8.4.3 | mandatory | implemented | `panweave-aps::tables` | Duplicate rejection using source address + APS counter within apsDuplicateRejectionTimeout | `duplicate_rejection_expiry_and_eviction`, `acked_unicast_is_delivered_once_and_confirmed` | Entry lifetime is apscAckWaitDuration x (1 + apscMaxFrameRetries); frames tunnelled to an unauthorized device bypass the table (§4.6.3.7.3). |
| PW-R23-APS-007 | §2.2.8.2, §2.2.8.3 | mandatory | implemented | `panweave-aps::tables` | Binding table and indirect (bound) transmission to unicast/group destinations; group table membership and group delivery to endpoints | `binding_table_bind_unbind`, `group_table_membership`, `group_delivery_and_loopback`, `bound_transmission_fans_out` |  |
| PW-R23-APS-008 | §2.2.4.2 | mandatory | implemented | `panweave-aps::tables` | APSME management: bind/unbind, add/remove group, get/set AIB | `binding_table_bind_unbind`, `group_table_membership` | Bind / unbind and group add / remove are binding and group table operations (panweave-aps::tables, driven by the ZDO front-ends in panweave-zdo and the Groups cluster); APSME-GET / APSME-SET are direct field access on the public Aib struct, the sans-I/O equivalent of the attribute primitives. |
| PW-R23-APS-009 | §2.2.9 | mandatory | implemented | `panweave-types::status` | APS status values | `data_header_round_trip` |  |
| PW-R23-APS-010 | Annex G | optional | implemented | `panweave-aps::interpan` | Inter-PAN transmission and reception with stub NWK header and inter-PAN APS header | `round_trips_and_detects_inter_pan`, `secured_frames_round_trip_between_link_key_holders`, `factory_new_remote_starts_a_network_through_a_lamp_and_adds_another`, `inter_pan_frames_cross_networks_and_may_be_secured` | The stub NWK header and inter-PAN APS header (unicast, broadcast, group delivery 0b11, Security / ACK Request with APS counter / Extended Header with fragmentation) are encoded and decoded, secured frames are protected and unprotected with the APS link key of the partner, MacService::data_request_inter_pan transmits with an explicit source PAN and the extended source address. The runtime routes received inter-PAN frames (NWK frame type 0b11) of the Touchlink Commissioning cluster to touchlink and every other one to the application as StackEvent::InterPanData (INTRP-DATA.indication, unprotected when secured); Stack::inter_pan_request is INTRP-DATA.request over broadcast, device, network-address and group targets with optional link-key security for device targets. Inter-PAN fragmentation is decoded but not used for transmission (frames fit one MAC frame). |
| PW-R23-APS-011 | §2.2.8.1 | mandatory | implemented | `panweave-runtime::persist` | APS persistent data: binding table, group table, designated coordinator, channel mask list, use extended PAN ID, use insecure join, descriptors, network manager address survive reset | `end_device_and_coordinator_survive_reboots` | Binding and group tables and the network manager address are persisted and restored; descriptors are compile-time application data re-registered on boot; apsDesignatedCoordinator and apsChannelMaskList are seeded from the configuration, apsUseExtendedPANID is learnt on formation / joining and restricts later network selection (JoinParams.extended_pan_id), apsUseInsecureJoin is kept for the application, and the four are stored in the AIB record and restored on warm start. apsChannelMaskList covers channel page 0. |
| PW-R23-SEC-032 | §4.4.12.1 | mandatory | implemented | `panweave-runtime::persist` | Persistence of security-related AIB values: apsTrustCenterAddress and every apsDeviceKeyPairSet entry are stored across reboot; IncomingFrameCounter, Timeout and VerifiedFrameCounter are not persisted | `end_device_and_coordinator_survive_reboots` | The outgoing frame counter of each entry is restored from its last persisted reservation bound so that values are never reused; the incoming counter is stored for freshness after reboot but reset by the peer's key verification as usual. The record set is indexed from the AIB record because the Storage trait has no enumeration. |
| PW-R23-SEC-033 | §4.6.3.8, §2.4.3.4.8, §2.4.4.4.8 | mandatory | implemented | `panweave-security::challenge` | APS frame counter verification: VerifiedFrameCounter per key-pair entry (false after reboot), UNVERIFIED_FRAME_COUNTER drops the frame and triggers Security_Challenge_req; challenge key/nonce derivation and CCM MIC-64 over the response TLV; response validation sets the incoming counter and marks it verified | `key_derivation_matches_the_worked_example`, `mic_depends_on_every_input`, `rebooted_trust_center_synchronizes_frame_counters` | One outstanding challenge at a time (apsChallengeTargetEui64 / apsChallengeValue), rate limited by apsChallengePeriodTimeoutSeconds; the frame counter synchronization capability is advertised in the Link-Key Features TLV of the Trust Center link key transport and assumed for negotiating partners. The Security_Challenge_rsp carries a status octet (ADR-0009). |
| PW-R23-NWK-068 | §3.6.10.7, §3.6.10.8 | mandatory | implemented | `panweave-runtime::persist` | Parent routers persist end-device neighbor entries and give every child a full timeout period after reboot; end devices should send a keep-alive immediately after reboot | `end_device_and_coordinator_survive_reboots` | Restored children start with timeout counter = device timeout. A rebooted end device performs a secured rejoin, which doubles as the initial keep-alive. |
| PW-R23-AF-001 | §2.3.2.3 | mandatory | implemented | `panweave-zdo::descriptor` | Node descriptor: logical type, complex/user descriptor availability (deprecated), APS flags, frequency band, MAC capability, manufacturer code, max buffer size, max incoming/outgoing transfer size, server mask (incl. stack compliance revision), descriptor capability | `node_descriptor_round_trip` |  |
| PW-R23-AF-002 | §2.3.2.4 | mandatory | implemented | `panweave-zdo::descriptor` | Power descriptor: current mode, available sources, current source, current level | `power_descriptor_round_trip` |  |
| PW-R23-AF-003 | §2.3.2.5 | mandatory | implemented | `panweave-zdo::descriptor` | Simple descriptor: endpoint, profile id, device id, device version, input and output cluster lists | `simple_descriptor_round_trip` |  |
| PW-R23-AF-004 | §2.3.4 | mandatory | implemented | `panweave-runtime::stack` | Application behaviour on PAN ID conflict | `network_manager_changes_the_pan_id_explicitly` | The application sees StackEvent::PanIdConflictReport for legacy reports and Stack::pan_id_conflicts() for the local count; it alone decides to call Stack::change_pan_id() (ADR-0010). Periodic querying of routers is left to the application. |
| PW-R23-ZDP-001 | §2.4.2, §2.4.5 | mandatory | implemented | `panweave-zdo::zdp` | ZDP frame: transaction sequence number followed by payload; cluster id = request, response = request \| 0x8000; status enumeration | `cluster_helpers_and_status`, `management_requests` |  |
| PW-R23-ZDP-002 | §2.4.3.1.1, §2.4.3.1.2, §2.4.4.1.1, §2.4.4.1.2 | mandatory | implemented | `panweave-zdo::layer` | NWK_addr_req / IEEE_addr_req with single/extended request types and their responses including associated device list | `address_requests_and_responses`, `address_discovery_with_children` |  |
| PW-R23-ZDP-003 | §2.4.3.1.3–§2.4.3.1.6, §2.4.4.1.3–§2.4.4.1.6 | mandatory | implemented | `panweave-zdo::layer` | Node_Desc, Power_Desc, Simple_Desc, Active_EP requests and responses | `descriptor_requests_and_responses`, `descriptor_discovery` |  |
| PW-R23-ZDP-004 | §2.4.3.1.7, §2.4.4.1.7 | mandatory | implemented | `panweave-zdo::layer` | Match_Desc_req/rsp with profile and cluster list matching; broadcast handling | `descriptor_discovery` |  |
| PW-R23-ZDP-005 | §2.4.3.1.11 | mandatory | implemented | `panweave-zdo::layer` | Device_annce broadcast on join/rejoin with short address, IEEE address, capability; receivers update address map and detect conflicts | `announce_and_binding_frames`, `device_announce_and_timeouts`, `duplicate_addresses_are_detected_and_reassigned` | Codec, broadcast on join / rejoin / re-addressing, and receipt through Nwk::on_device_announce: a lost end device child announcing under another address is re-addressed with an unsolicited Rejoin Response (step 2a), an address already held by another neighbour or address map entry raises the address conflict procedure (step 2b), otherwise the address map and neighbour table take the announced address. |
| PW-R23-ZDP-006 | §2.4.3.1.12, §2.4.4.1.9 | mandatory | implemented | `panweave-zdo::layer` | Parent_annce / Parent_annce_rsp for child address reconciliation after router restart | `announce_and_binding_frames`, `rebooted_parent_announces_children_and_drops_the_claimed_one` | A router or coordinator that resumes after a reboot arms apsParentAnnounceTimer (base 10 s plus up to 10 s of jitter) and broadcasts Parent_annce with every end device child, one message per timer expiry when they do not fit; a router that has one of them as its child answers with Parent_annce_rsp and the announcer drops that neighbor entry (StackEvent::ChildClaimed). Keepalives received meanwhile are not excluded from later messages. |
| PW-R23-ZDP-007 | §2.4.3.1.14, §2.4.4.1.10 | optional | implemented | `panweave-zdo::layer` | System_Server_Discovery_req/rsp with server mask | `descriptor_requests_and_responses` |  |
| PW-R23-ZDP-008 | §2.4.3.2.2, §2.4.3.2.3, §2.4.3.2.12, §2.4.4.2 | mandatory | implemented | `panweave-zdo::layer` | Bind_req, Unbind_req, Clear_All_Bindings_req and responses with unicast/group destination address modes | `announce_and_binding_frames`, `binding_and_restricted_mode` |  |
| PW-R23-ZDP-009 | §2.4.3.3.2, §2.4.4.3.2 | mandatory | implemented | `panweave-zdo::layer` | Mgmt_Lqi_req/rsp neighbor table listing with start index and entry format | `management_frames`, `management_requests` |  |
| PW-R23-ZDP-010 | §2.4.3.3.3, §2.4.4.3.3 | optional | implemented | `panweave-zdo::layer` | Mgmt_Rtg_req/rsp routing table listing | `management_frames` |  |
| PW-R23-ZDP-011 | §2.4.3.3.4, §2.4.4.3.4 | optional | implemented | `panweave-zdo::layer` | Mgmt_Bind_req/rsp binding table listing | `announce_and_binding_frames`, `management_requests` |  |
| PW-R23-ZDP-012 | §2.4.3.3.5, §2.4.4.3.5 | mandatory | implemented | `panweave-zdo::layer` | Mgmt_Leave_req/rsp with remove-children and rejoin flags; only accepted per policy (from TC or for self) | `management_frames`, `management_requests` | Acceptance follows apsZdoRestrictedMode (§2.4.3); the NWK leave policy (nwkLeaveRequestAllowed) is applied by the NWK layer. |
| PW-R23-ZDP-013 | §2.4.3.3.7, §2.4.4.3.7 | mandatory | implemented | `panweave-zdo::layer` | Mgmt_Permit_Joining_req/rsp with duration and TC significance; broadcast to routers | `management_frames`, `management_requests`, `trust_center_sets_the_network_wide_beacon_appendix` | Trust Center policy applied for remote requests (§4.7.3.4); a Trust Center's request carries the Beacon Appendix Encapsulation with Supported Key Negotiation Methods and Fragmentation Parameters, and receiving routers replace nwkNetworkWideBeaconAppendixTLVs with it (§2.4.3.3.7.2). |
| PW-R23-ZDP-014 | §2.4.3.3.9, §2.4.4.3.9 | mandatory | implemented | `panweave-runtime::pump` | Mgmt_NWK_Update_req/notify: energy scan request, channel change, network manager change, update id; energy scan results and counters | `network_manager_energy_scan_and_channel_change` | Energy scan (ScanDuration 0–5, unicast only, ScanCount repeats) answered with Mgmt_NWK_Update_notify carrying per-channel energy and the NIB transmission counters; channel change (0xfe) honours nwkNextChannelChange, single supported channel and nwkUpdateId; 0xff updates apsChannelMaskList and nwkManagerAddr. Mgmt_NWK_Enhanced_Update_req answers NOT_SUPPORTED. |
| PW-R23-ZDP-015 | §2.4.3.3.10, §2.4.3.3.11, §2.4.3.3.12 | optional | implemented | `panweave-zdo::zdp` | Mgmt_NWK_Enhanced_Update, Mgmt_NWK_IEEE_Joining_List, Mgmt_NWK_Beacon_Survey and responses | `network_management_r23_codecs`, `joining_list_gates_association_and_is_read_and_pushed`, `enhanced_update_scans_a_single_page_and_end_device_surveys_beacons`, `router_reports_interference_and_the_manager_moves_the_network` | Codecs for Mgmt_NWK_Enhanced_Update_req / _notify (with the Channel List Structure of §3.2.2.2.1), Mgmt_NWK_IEEE_Joining_List_req / _rsp, Mgmt_NWK_Unsolicited_Enhanced_Update_notify and Mgmt_NWK_Beacon_Survey_req / _rsp with their TLVs. The ZDO answers the joining list request from the joining policy and IEEE list held by the NWK layer (INVALID_INDEX past the end, broadcasts dropped, unsolicited broadcast supported), applies received responses per §2.4.4.3.11.2, and the policy gates association and initial commissioning joins. The runtime services Mgmt_NWK_Enhanced_Update_req per §2.4.3.3.9.2 over the channel list of its single 2.4 GHz interface (channel change: one channel across all pages on a supported page; attribute change: the whole list into apsChannelMaskList; energy scan: a single supported page; INV_REQUESTTYPE otherwise) and a Beacon Survey with a legacy or, per the configuration bitmask, enhanced active scan counting beacons (results, potential parents ordered by LQA, PAN ID conflict report; coordinators refuse with NOT_PERMITTED, a missing TLV is MISSING_TLV). Mgmt_NWK_Unsolicited_Enhanced_Update_notify is sent by the Annex E interference reporting and delivered to a network manager as StackEvent::InterferenceReport. |
| PW-R23-ZDP-016 | §2.4.3.4.1, §2.4.4.4.1 | mandatory | implemented | `panweave-zdo::security` | Security_Start_Key_Negotiation_req/rsp carrying key negotiation method and public point TLVs | `security_services`, `anonymous_key_negotiation_join_through_router`, `install_code_authenticated_negotiation` | Curve25519 Public Point TLV, SPEKE/AES-MMO-128 (protocol 1); SPEKE/SHA-256 (protocol 2) and CBKE are answered with NO_MATCH. |
| PW-R23-ZDP-017 | §2.4.3.4.2, §2.4.4.4.2 | mandatory | implemented | `panweave-zdo::security` | Security_Retrieve_Authentication_Token_req/rsp | `security_services`, `anonymous_key_negotiation_join_through_router` | The Trust Center hands out a random 128-bit passphrase once (PassphraseUpdateAllowed) and locks the entry when generating it rather than after the APS acknowledgement of the response (§2.4.3.4.2.4 step 10). |
| PW-R23-ZDP-018 | §2.4.3.4.3, §2.4.3.4.4, §2.4.4.4.3, §2.4.4.4.4 | mandatory | implemented | `panweave-zdo::security` | Security_Get_Authentication_Level_req/rsp, Security_Set_Configuration_req/rsp | `security_services` | Set_Configuration applies Configuration Parameters, Next PAN ID and Next Channel Change; Get_Authentication_Level reports InitialJoinMethod / ActiveLinkKeyType from the key-pair set. |
| PW-R23-ZDP-019 | §2.4.3.4.5, §2.4.3.4.6, §2.4.3.4.7 and responses | mandatory | implemented | `panweave-zdo::security` | Security_Get_Configuration, Security_Start_Key_Update, Security_Decommission | `security_services`, `anonymous_key_negotiation_join_through_router`, `rebooted_trust_center_synchronizes_frame_counters` | Get_Configuration serves Configuration Parameters, Next PAN ID / Channel, PAN ID Conflict Report (reset on read), Supported Key Negotiation Methods, Router Information and Fragmentation Parameters; Start_Key_Update, Decommission (NO_MATCH stands in for NOT_FOUND, ADR-0007) and Security_Challenge are implemented. |
| PW-R23-ZDP-020 | §2.4.3.1.8–§2.4.3.1.10, §2.4.3.1.13–§2.4.3.1.23, §2.4.3.2.1, §2.4.3.2.4–§2.4.3.2.11, §2.4.3.3.1, §2.4.3.3.6, §2.4.3.3.8 | deprecated | implemented | `panweave-zdo::layer` | Deprecated ZDP commands (complex/user descriptors, discovery cache, bind backups, end device bind, direct join, cache) are not generated; requests receive NOT_SUPPORTED where a response is defined | `management_requests` |  |
| PW-R23-ZDP-021 | §2.4.4 | mandatory | implemented | `panweave-zdo::layer` | ZDP server: respond to unicast requests, respond to broadcast only when matching, apply NOT_SUPPORTED / INV_REQUESTTYPE / DEVICE_NOT_FOUND / INVALID_EP semantics | `descriptor_discovery`, `address_discovery_with_children`, `management_requests`, `device_announce_and_timeouts` |  |
| PW-R23-ZDO-001 | §2.5.2 | mandatory | implemented | `panweave-runtime` | Device and service discovery, network manager, binding manager, node manager, security manager objects with configuration attributes | `descriptor_discovery`, `binding_and_restricted_mode`, `management_requests`, `security_services`, `network_manager_energy_scan_and_channel_change`, `network_manager_changes_the_pan_id_explicitly` | The ZDO objects are realised as runtime modules rather than a separate objects module: device and service discovery in panweave-zdo::layer, the network manager in panweave-runtime::pump (Mgmt_NWK_Update, PAN ID change, energy scans), the binding manager in panweave-aps::tables with ZDP Bind/Unbind, the node manager in the Mgmt_* handlers and the security manager in panweave-runtime::dlk / panweave-security::trust_center. |
| PW-R23-ZDO-002 | §2.5.5 | mandatory | implemented | `panweave-zdo::config` | ZDO configuration attributes (permit join duration, network manager address, scan attempts, timeouts, etc.) | `defaults_follow_table_2_135`, `discovery_repeats_config_nwk_scan_attempts_times`, `rejoins_after_the_threshold_and_paces_retries`, `bind_requests_are_capped_at_config_max_bind` | ConfigAttributes carries the Table 2-135 defaults and the runtime applies them: :Config_NWK_Scan_Attempts and :Config_NWK_Time_btwn_Scans drive discovery retries; an end device counts parent link failures against :Config_Parent_Link_Retry_Threshold before rejoining and paces repeated parent-loss rejoins by :Config_Rejoin_Interval, doubling after each failure up to :Config_Max_Rejoin_Interval; Bind_req is refused with INSUFFICIENT_SPACE beyond :Config_Max_Bind; :Config_Permit_Join_Duration is the application default for Stack::permit_join_network. :Config_Max_Assoc is the NIB nwkMaxChildren. |
| PW-R23-SEC-001 | §4.5.1 | mandatory | implemented | `panweave-security::aux_header` | Auxiliary security header: security control (level, key identifier, extended nonce), frame counter, optional source address, optional key sequence number | `nwk_aux_header_round_trip_and_nonce`, `aps_aux_header_without_extended_nonce`, `security_level_properties` |  |
| PW-R23-SEC-002 | Annex A | mandatory | implemented | `panweave-security::ccm` | CCM* with 13-octet nonce (source address, frame counter, security control), security levels 0–7, M in {0,4,8,16}, L = 2 | `annex_c3_encryption_vector`, `annex_c4_decryption_vector_and_tamper_detection`, `mic_only_and_enc_only_round_trip`, `parameter_validation`, `multi_block_aad_and_message` |  |
| PW-R23-SEC-003 | Annex B.6, Annex C.5 | mandatory | implemented | `panweave-security::mmo` | AES-MMO (Matyas-Meyer-Oseas) 128-bit hash with specified padding; verified with Annex C vectors | `annex_c5_1_single_octet`, `annex_c5_2_sixteen_octets`, `annex_c5_3_to_6_long_messages` |  |
| PW-R23-SEC-004 | Annex B.1.4, Annex C.6 | mandatory | implemented | `panweave-security::mmo::hmac` | Keyed hash (HMAC construction over AES-MMO) used for key hashing and key verification | `annex_c6_hmac_vectors` |  |
| PW-R23-SEC-005 | §4.5.3 | mandatory | implemented | `panweave-security::key_hierarchy` | Key hierarchy: key-transport key and key-load key derived from link key by hashing with 0x00 and 0x02; hashed link key in transport-key protection | `derived_keys_differ_and_are_deterministic`, `install_code_known_answer_from_bdb` |  |
| PW-R23-SEC-006 | §4.3.1 | mandatory | implemented | `panweave-nwk::security` | NWK frame security: outgoing frames secured with active key and nwkSecurityLevel, incoming frames decrypted with key matching sequence number, incoming frame counter freshness per source, security failure causes drop | `secure_then_unsecure_with_freshness`, `counter_reservation_gates_transmission`, `secured_rejoin_after_reboot_keeps_address` | Cryptographic transform done; key selection and NIB integration follow in panweave-nwk. |
| PW-R23-SEC-007 | §4.3.4 | mandatory | implemented | `panweave-security::frame_counter` | Network frame counter requirements: monotonic outgoing counter persisted across resets, reset to zero only on key switch, incoming counters reset on key switch, request key update near exhaustion | `outgoing_counter_never_decreases_across_restart`, `reservation_requested_ahead_of_need_and_window_is_hard_limit`, `exhaustion_and_switch_reset_rule`, `incoming_freshness` |  |
| PW-R23-SEC-008 | §4.4.1 | mandatory | implemented | `panweave-aps::security` | APS frame security using link keys (data and command frames), key-transport/key-load key selection for transport key commands | `data_key_round_trip_with_and_without_extended_nonce`, `derived_keys_and_fallback_entry`, `unverified_key_cannot_encrypt_and_counter_needs_reservation`, `link_key_secured_data_round_trip` | Incoming frame-counter freshness is enforced for unique link keys only (§4.4.1.2 step 4); frames from partners whose counter is unverified are dropped and synchronized with a challenge (PW-R23-SEC-033). |
| PW-R23-SEC-009 | §4.4.11.1 | mandatory | implemented | `panweave-aps::command` | Transport Key command: standard network key (key, sequence number, destination and source addresses), trust center link key, application link key with partner and initiator flag | `transport_key_variants`, `trust_center_join_and_link_key_update` |  |
| PW-R23-SEC-010 | §4.4.11.2 | mandatory | implemented | `panweave-aps::command` | Update Device command: device address, short address, status (standard secured/unsecured rejoin, left, TC rejoin) | `device_management_commands`, `trust_center_join_and_link_key_update` |  |
| PW-R23-SEC-011 | §4.4.11.3 | mandatory | implemented | `panweave-aps::command` | Remove Device command with target address | `device_management_commands` |  |
| PW-R23-SEC-012 | §4.4.11.4 | mandatory | implemented | `panweave-aps::command` | Request Key command: key type (application link key, TC link key), partner address | `device_management_commands`, `trust_center_join_and_link_key_update` |  |
| PW-R23-SEC-013 | §4.4.11.5 | mandatory | implemented | `panweave-aps::command` | Switch Key command with sequence number | `device_management_commands`, `unauthorized_joiner_ignores_data_and_unencrypted_key` |  |
| PW-R23-SEC-014 | §4.4.11.6 | mandatory | implemented | `panweave-aps::layer::cmd` | Tunnel command wrapping an APS command for a joining device via its parent | `device_management_commands`, `trust_center_join_and_link_key_update`, `router_and_sleepy_end_device_with_router_failure` |  |
| PW-R23-SEC-015 | §4.4.11.7, §4.4.11.8 | mandatory | implemented | `panweave-aps::layer::cmd` | Verify Key (key type, source address, initiator verify-key hash) and Confirm Key (status, key type, destination address) | `device_management_commands`, `trust_center_join_and_link_key_update` |  |
| PW-R23-SEC-016 | §4.4.11.9, §4.4.11.10 | mandatory | implemented | `panweave-aps::layer::cmd` | Relay Message Downstream/Upstream commands for relaying APS commands to/from joiners through a router | `relay_message_tlv`, `relayed_frames_reach_the_trust_center_and_back` |  |
| PW-R23-SEC-017 | §4.4.12, §4.4.13 | mandatory | implemented | `panweave-security::material` | Security AIB: device key pair set (apsDeviceKeyPairSet) with key attributes, incoming/outgoing frame counters, passphrase, TC address, trust center policies | `network_keys_two_slots_and_switch`, `link_key_table_bounded`, `debug_output_redacts_keys` |  |
| PW-R23-SEC-018 | §4.6.3.2 | mandatory | implemented | `panweave-aps::layer::cmd` | Joining device authentication: after MAC association receive Transport Key (network key) protected with pre-configured/derived link key; start authentication timer; leave on failure | `trust_center_join_and_link_key_update`, `unauthorized_joiner_ignores_data_and_unencrypted_key`, `coordinator_and_end_device_full_stack` | The joiner learns apsTrustCenterAddress from the Transport Key source and renames its pre-installed key entry; unencrypted transport keys are refused while unauthorized when requireLinkKeyEncryptionForApsTransportKey is set. Forwarding of an APS-encrypted Transport Key by a parent without the Tunnel command (UseParent without TunnelCommand) is not supported because the parent cannot decrypt it; see docs/adr. |
| PW-R23-SEC-019 | §4.6.3.2, §4.7 | mandatory | implemented | `panweave-runtime::pump` | Trust Center handling of Update Device: apply policy (allow joins, install code requirement, rejoin policy), send Transport Key (tunnelled via parent when joiner is not a neighbor), start TCLK exchange timer, Remove Device on rejection | `trust_center_join_and_link_key_update`, `coordinator_and_end_device_full_stack`, `router_and_sleepy_end_device_with_router_failure` | The runtime evaluates TrustCenterPolicy on join indications and Update Device, creates the well-known key entry when required, sends the network key directly to neighbour joiners or tunnelled through the parent, and authenticates the child once the Transport Key is confirmed. Removal on rejection uses NLME-LEAVE. |
| PW-R23-SEC-020 | §4.6.3.4 | mandatory | implemented | `panweave-runtime::provision` | Network key update: TC broadcasts new key via Transport Key (nwkKeyType alternate), then Switch Key after nwkNetworkBroadcastDeliveryTime; devices store alternate key and switch on Switch Key; frame counters reset | `trust_center_rotates_the_network_key`, `zdd_keeps_past_network_keys_for_limited_authorization` | Stack::update_network_key on the Trust Center installs the alternate key ((N+1) mod 256), broadcasts the Transport Key to rx-on devices, unicasts it to its own rx-off children, and after nwkNetworkBroadcastDeliveryTime broadcasts the Switch Key and switches itself; routers unicast a received broadcast network key to their rx-off children (section 4.4.2.3 last paragraph, once, held by the MAC until the child polls); devices store the alternate key, switch on the Switch Key from the Trust Center and report StackEvent::NetworkKeySwitched with the retired sequence number, which stays in its slot until the next update. A sleepy end device, which never hears the 0xFFFD Switch Key broadcast, switches when its parent first uses the alternate key (ADR-0014). The unicast key update to a device list (section 4.6.3.4.1 second option) is not offered. |
| PW-R23-SEC-021 | §4.4.5, §4.6.3.4.2 | mandatory | not-applicable | `panweave-security::procedures::key_update` | Network key recovery: after Switch Key with unknown sequence number, request via Request Key | — | R23.2 defines Request Key only for Trust Center (0x04) and application (0x02) link keys; a Switch Key naming an unknown sequence number is ignored (Nwk::switch_network_key returns false) and a device that lost the network key recovers through a Trust Center rejoin (BDB 3.1 §10.1, implemented in panweave-bdb). |
| PW-R23-SEC-022 | §4.6.3.6 | mandatory | implemented | `panweave-runtime::pump` | Device leave: TC sends Remove Device to parent, parent issues NWK Leave; device leaving reports Update Device (device left) | `trust_center_removes_a_device_through_its_parent` | Stack::remove_device issues a NWK Leave to a direct child or an APS Remove Device to the parent; a router accepts Remove Device from the Trust Center for itself or a child, reports departed children with Update Device (Device Left) — on the child's own leave announcement and after a requested leave was confirmed (an end device told to leave by its parent sends no announcement, §3.6.1.11.3.1) — and the Trust Center surfaces DeviceLeft to the application. |
| PW-R23-SEC-023 | §4.7.1, §4.7.3 | mandatory | implemented | `panweave-security::trust_center` | Trust Center policies: allowJoins, allowRejoins, allowTrustCenterLinkKeyRequests, allowApplicationKeyRequests, useWhiteList/joining list, requireInstallCodesOrPresetPassphrase, allowRemoteTcPolicyChange, network key update method/period, allowVirtualDevices | `unsecured_join_policy`, `key_negotiation_join_policy`, `rejoin_policy`, `tclk_request_policy`, `app_key_and_remote_policy` |  |
| PW-R23-SEC-024 | §4.7.2, §4.9.3, §4.9.4 | mandatory | implemented | `panweave-runtime::pump` | Trust Center link keys: well-known global key, install-code derived keys, unique TCLK per device, key attributes provisional/unverified/verified, Verify Key / Confirm Key exchange after receiving a new TCLK | `trust_center_join_and_link_key_update`, `coordinator_and_end_device_full_stack` | Well-known global key entries, unique per-device keys generated by the Trust Center on Request Key, verify/confirm transitions provisional -> unverified -> verified. Install-code derived keys are supported by the key material (link_key_from_install_code) but the TC-side install-code provisioning API is still to be exposed. |
| PW-R23-SEC-025 | §4.4.9, §4.9.6, Annex J | mandatory | implemented | `panweave-runtime::dlk` | Dynamic Link Key negotiation: ECDHE (Curve25519) with SPEKE using passphrase or well-known passphrase, AES-MMO based KDF, key confirmation; results in unverified TCLK | `annex_c7_1_1_well_known_psk_vector`, `annex_c7_1_2_passphrase_vector_generator`, `non_contributory_points_are_rejected`, `random_ephemerals_agree`, `anonymous_key_negotiation_join_through_router`, `install_code_authenticated_negotiation`, `wrong_passphrase_fails_and_restores_the_entry` | Dynamic Key Negotiation Joining end to end (Start_Key_Update → Start_Key_Negotiation via Relay Message → Verify / Confirm Key → Transport Key) with anonymous and install-code secrets, entry backup/restore and apsSecurityTimeOutPeriod deadlines; on-network TCLK re-negotiation after Node_Desc_rsp (BDB §7.2) is not implemented (Request Key is used after a symmetric join). The dlk feature enables Curve25519. |
| PW-R23-SEC-026 | §4.9.7 | mandatory | implemented | `panweave-runtime::dlk` | Passphrase update after joining via Security_Set_Configuration / symmetric passphrase TLV | `anonymous_key_negotiation_join_through_router` | Passphrase obtained with Security_Retrieve_Authentication_Token after a negotiated join; Security_Set_Configuration does not carry a passphrase in R23.2 (the symmetric passphrase TLV is only used in the token response). |
| PW-R23-SEC-027 | §4.8 | mandatory | implemented | `panweave-aps::layer::cmd` | Distributed security network: TC address 0xFFFF…FF, router issues network key on join, distributed global link key usage | `any_router_removes_a_node_with_mgmt_leave`, `factory_new_remote_starts_a_network_through_a_lamp_and_adds_another` | Distributed-network Transport Key source 0xFFFF...FF handling and apsTrustCenterAddress semantics are in the APS; a router of a distributed network hands the network key to a joiner itself under the distributed security global link key (the preconfigured global entry, else the development key), installing the per-device entry it needs. |
| PW-R23-SEC-028 | §4.7.4 | optional | implemented | `panweave-runtime::swap_out` | Trust Center swap-out procedure | `replacement_trust_center_takes_over_from_a_hashed_backup` | Backup of the device key-pair set with the AES-MMO hashed TrustCenterSwapOutLinkKey (Table 4-43/4-44) and its restoration on a replacement Trust Center; on the node the APS layer retries a failed command during a Trust Center rejoin under the hashed key (§4.7.4.1.2.7), moves the entry to the new Trust Center address, reports TrustCenterSwapped and the runtime requests a fresh link key before APS-secured messaging (step 8). The application drives the rejoin after the keep-alive fails. |
| PW-R23-SEC-029 | §4.9.1 | mandatory | implemented | `panweave-runtime::dlk` | Joining device policies: preconfigured key type, TCLK requirement, behaviour on failed TCLK exchange | `wrong_passphrase_fails_and_restores_the_entry`, `install_code_authenticated_negotiation` | Preconfigured key type is StackConfig::preconfigured_link_key; a failed or timed-out negotiation restores the previous key-pair entry (§4.4.9) and the joiner leaves after apsSecurityTimeOutPeriod without a network key. |
| PW-R23-SEC-030 | Annex C | informative | implemented | `panweave-security tests` | Cryptographic test vectors for AES-MMO, HMAC, CCM* and key agreement are used as known-answer tests | `annex_c3_encryption_vector`, `annex_c5_1_single_octet`, `annex_c6_hmac_vectors`, `annex_c7_1_1_well_known_psk_vector` |  |
| PW-R23-SEC-031 | §4.6.3.7, Annex I.4.12 | mandatory | implemented | `panweave-runtime::dlk` | Device interview state: TC withholds authorization until interview complete; device capability extension TLV | `interview_holds_the_network_key_until_admitted`, `interview_rejection_removes_the_joiner`, `anonymous_key_negotiation_join_through_router` | Authorization is withheld until the negotiated key is verified (section 4.7.3.3 steps 4-10); with TrustCenterPolicy::interview_joiners the verified joiner is reported (StackEvent::JoinerVerified) and the network key waits for Stack::admit_joiner, while Stack::reject_joiner drops its key-pair entry and removes it (the pending join otherwise expires with the security timeout). The application exchanges of the interview itself are implementation specific (BDB 3.1 section 9.9). The Device Capability Extension TLV is parsed; virtual devices are not admitted. |
| PW-R23-MAC-001 | Annex D.2–D.9 | mandatory | implemented | `panweave-mac` | MAC usage: beacon-order 15 non-beacon operation, association capability information, frame version, beacon payload placement, CSMA backoff exponent defaults, recommended scan duration | `intra_pan_data_header_compresses_source_pan`, `beacon_round_trip`, `mac_commands_round_trip`, `association_handshake_joiner_side`, `association_indication_and_indirect_response_parent_side`, `active_scan_sends_beacon_requests_per_channel_and_confirms` | Frame version 0 on transmit (D.6), association addressing (D.4), backoff defaults (D.8) and scan duration (D.9) implemented; Annex D.11 enhanced beacons tracked separately. |
| PW-R23-MAC-002 | Annex D.11.1 | optional | implemented | `panweave-mac::ie` | Enhanced beacon request / enhanced beacon with Zigbee payload IE | `enhanced_beacon_request_of_figure_d1`, `rejoin_request_round_trips_and_unknown_ies_are_skipped`, `enhanced_beacon_round_trips`, `joins_and_rejoins_through_enhanced_beacons`, `joining_list_filters_enhanced_beacon_requests` | Payload IE list handling, the MLME nested EB Filter IE and the Zigbee Payload IE (OUI 4A-19-1B) with the Rejoin, TX Power and EB Payload sub-IEs (Table D-5), checked against the Figure D-1 frame. Behind MacServiceConfig::enhanced_beacons and NwkConfig::enhanced_beacon_requests: network discovery sends EBRs (EB Filter IE with Permit Joining On when joining, Rejoin IE when rejoining, TX Power IE at the maximum power); routers answer per the filter, the mibJoiningPolicy / mibJoiningIeeeList mirrored into the MAC PIB and the extended PAN ID of a rejoin (D.11.1.2, D.11.1.3), with an Enhanced Beacon whose EB Payload IE is presented to the NWK as a standard beacon. The percent filter is not applied (every eligible router answers) and the mibIeeeExpiryInterval countdown is not run (the list lives until the Trust Center replaces it). |
| PW-R23-MAC-003 | Annex D.11.2 | optional | implemented | `panweave-mac::power` | MAC power control support | `adjusts_from_maximum_and_clamps`, `unnegotiated_entries_expire_and_are_evicted_first`, `joins_and_rejoins_through_enhanced_beacons` | The Power Control Information Table (D.11.2.3: per-link TX power and last RSSI, NWK-negotiated flag set when the join confirms the link, 10 s expiry of unconfirmed links) with configurable limits and OPTRSSI (D.11.2.4.4, D.11.2.4.2) is kept by the MAC service and sets TxOptions::tx_power_dbm for every unicast to a known link (D.11.2.4.5); RSSI accompanies every reception (D.11.2.1.1). The TX Power IE of the EBR/EB exchange negotiates the initial link power (D.11.2.4.2). Beacons and acknowledgements go out at the maximum power (D.11.1.5 second bullet: the highest-known-power-plus-6-dB rule is not applied). |
| PW-R23-MAC-004 | Annex D.12–D.14 | optional | not-applicable | `vendor radio backends` | Sub-GHz FSK PHY/MAC (GB and regional): duty cycle, LBT | — | PHY-specific; outside the hardware-independent core. |
| PW-R23-MAC-005 | Annex E, Annex F | optional | partially-implemented | `panweave-runtime::agility` | Network manager as channel manager for interference reporting; multiple frequency band usage | `router_reports_interference_and_the_manager_moves_the_network` | Annex E: routers and coordinators judge their unicast failure rate (nwkTxTotal / nwkTxFailures against StackConfig::interference) every ten minutes, run an energy scan across the 2.4 GHz band and report a noisier-than-alternatives channel to nwkManagerAddr with Mgmt_NWK_Unsolicited_Enhanced_Update_notify (no more than four an hour, counters restarted); the network manager surfaces reports as StackEvent::InterferenceReport (the channel-change decision is the application's, as the annex leaves it to implementers) and Stack::change_network_channel broadcasts Mgmt_NWK_Update_req with ScanDuration 0xfe, increments nwkUpdateId, switches after nwkNetworkBroadcastDeliveryTime and enforces apsChannelTimer (1 hour by default). Receivers change channel per §2.4.3.3.9.2. Annex F (multiple frequency bands, sub-GHz PHYs) is not implemented: one 2.4 GHz interface. |
| PW-R23-MAC-006 | Annex K | optional | not-implemented | `panweave-nwk::routing` | Provisional routing improvements (extended route information TLVs) | — | Provisional; not planned until stabilised. |

