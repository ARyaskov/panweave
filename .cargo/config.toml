[alias]
# Full quality gate used by CI and before every commit.
gate = "run --quiet --package panweave-xtask --"

[target.'cfg(all(target_arch = "arm", target_os = "none"))']
rustflags = ["-C", "link-arg=-Tlink.x"]
